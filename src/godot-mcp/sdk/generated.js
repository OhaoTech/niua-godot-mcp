// AUTO-GENERATED by scripts/gen-sdk.mjs — DO NOT EDIT.
// Regenerate: `node scripts/gen-sdk.mjs`. Drift is enforced by test.

export function buildNamespaces(call) {
  return {
    "animation": {
      /**
     * Create or update a basic AnimationTree StateMachine wired to an AnimationPlayer.
     * @param {object} [args]
     * @param {boolean} [args.active] Activate the AnimationTree after creation. Defaults to true.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.parentPath] Parent path used when creating the AnimationTree. Empty string means the scene root.
     * @param {string} args.playerPath AnimationPlayer path used by the AnimationTree.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {Array} args.states
     * @param {Array} [args.transitions]
     * @param {string} [args.treeName] AnimationTree name used when treePath is omitted. Defaults to AnimationTree.
     * @param {string} [args.treePath] Existing AnimationTree path to update.
     */
      "create_animation_tree_state_machine": (args = {}) => call("create_animation_tree_state_machine", args),
      /**
     * Read an AnimationPlayer playback state and available animations.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.playerPath AnimationPlayer node path under the edited scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "get_animation_state": (args = {}) => call("get_animation_state", args),
      /**
     * Instance an imported PackedScene that contains animations into the edited scene and report nested AnimationPlayers.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.name] Optional name for the instanced scene root.
     * @param {string} [args.parentPath] Parent path under the edited scene root. Empty string means the scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} args.scenePath Imported PackedScene path, including .glb, to instance into the edited scene.
     */
      "instance_animated_scene": (args = {}) => call("instance_animated_scene", args),
      /**
     * List AnimationPlayer animations in the edited scene or inside an imported PackedScene such as a GLB.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.nodePath] Optional scene node subtree to scan for AnimationPlayers.
     * @param {string} [args.playerPath] Optional AnimationPlayer node path to inspect.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} [args.scenePath] Optional imported PackedScene path, including .glb, to inspect without instancing into the edited sc
     */
      "list_animations": (args = {}) => call("list_animations", args),
      /**
     * Play an AnimationPlayer animation in the visible Godot editor.
     * @param {object} [args]
     * @param {string} args.animationName Animation name to play.
     * @param {number} [args.customBlend] Optional custom blend duration.
     * @param {number} [args.customSpeed] Optional playback speed multiplier.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {boolean} [args.fromEnd] Play from the end.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.playerPath AnimationPlayer node path under the edited scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "play_animation": (args = {}) => call("play_animation", args),
      /**
     * Stop an AnimationPlayer animation in the visible Godot editor.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {boolean} [args.keepState] Keep the current animated values when stopping. Defaults to false.
     * @param {string} args.playerPath AnimationPlayer node path under the edited scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "stop_animation": (args = {}) => call("stop_animation", args),
      /**
     * Travel an AnimationTree StateMachine playback to a named state.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} args.state State-machine state to travel to.
     * @param {string} args.treePath AnimationTree node path under the edited scene root.
     */
      "travel_animation_tree": (args = {}) => call("travel_animation_tree", args),
      /**
     * Create or replace an AnimationPlayer animation with property or transform tracks and keyframes.
     * @param {object} [args]
     * @param {string} args.animationName Animation name to create or replace.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.length] Animation length in seconds. Defaults to the last keyframe time.
     * @param {string} [args.loopMode] Animation loop mode. Defaults to none.
     * @param {string} [args.parentPath] Parent path used when creating the AnimationPlayer. Empty string means the scene root.
     * @param {string} [args.playerName] AnimationPlayer name used when playerPath is omitted. Defaults to AnimationPlayer.
     * @param {string} [args.playerPath] Existing AnimationPlayer node path under the edited scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} [args.rootNodePath] AnimationPlayer root_node path. Defaults to '..' for a player under the scene root.
     * @param {Array} args.tracks
     */
      "upsert_animation": (args = {}) => call("upsert_animation", args),
    },
    "audio": {
      /**
     * Create an AudioStreamPlayer with an AudioStreamGenerator stream and route it to a named bus.
     * @param {object} [args]
     * @param {boolean} [args.autoplay] Set AudioStreamPlayer.autoplay.
     * @param {string} [args.busName] Audio bus assigned to AudioStreamPlayer.bus.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {object} [args.generator]
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.name]
     * @param {string} [args.parentPath] Parent path under the edited scene root. Empty string means the scene root.
     * @param {boolean} [args.play] Start playback immediately in the editor after creation.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {number} [args.volumeDb] Player volume in decibels.
     */
      "create_audio_stream_player": (args = {}) => call("create_audio_stream_player", args),
      /**
     * Read Godot's current audio bus layout, including effects, volume, mute, and send routing.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "list_audio_buses": (args = {}) => call("list_audio_buses", args),
      /**
     * Remove a non-Master audio bus and persist the bus layout.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.layoutPath] Bus layout resource path. Defaults to res://default_bus_layout.tres.
     * @param {string} args.name Audio bus name to remove. Master cannot be removed.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.save] Persist the generated bus layout. Defaults to true.
     */
      "remove_audio_bus": (args = {}) => call("remove_audio_bus", args),
      /**
     * Create, rename, route, mute, or set volume for an audio bus, then persist the bus layout.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.fromName] Optional existing bus name to rename to name.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.index] Optional insertion index when creating a new bus.
     * @param {string} [args.layoutPath] Bus layout resource path. Defaults to res://default_bus_layout.tres.
     * @param {boolean} [args.muted] Whether the bus is muted.
     * @param {string} args.name Audio bus name to create or update.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.save] Persist the generated bus layout. Defaults to true.
     * @param {string} [args.send] Destination bus name. Defaults to Master for new buses.
     * @param {number} [args.volumeDb] Bus volume in decibels.
     */
      "upsert_audio_bus": (args = {}) => call("upsert_audio_bus", args),
      /**
     * Add or update a curated AudioEffectReverb or AudioEffectLimiter on an audio bus.
     * @param {object} [args]
     * @param {string} args.busName Audio bus that owns the effect.
     * @param {number} [args.effectIndex] Existing effect index to replace/update. Omit to append.
     * @param {string} args.effectKind Curated AudioEffect kind.
     * @param {boolean} [args.enabled] Whether the effect starts enabled.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.layoutPath] Bus layout resource path. Defaults to res://default_bus_layout.tres.
     * @param {object} [args.parameters] Effect properties such as wet, room_size, threshold_db, or ceiling_db.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.save] Persist the generated bus layout. Defaults to true.
     */
      "upsert_audio_bus_effect": (args = {}) => call("upsert_audio_bus_effect", args),
    },
    "catalog": {
      /**
     * Navigate the full tool catalog without paying for a flat listing. No args returns the root map of domains; { domain } lists that domain's tools with tier and summary; { name } returns one tool's full description and input schema. Always describes the whole catalog, even for tools hidden by the active profile.
     * @param {object} [args]
     * @param {string} [args.domain] List this domain's tools (name, tier, one-line summary).
     * @param {string} [args.name] Return this tool's full description and input schema. Takes precedence over domain.
     */
      "describe_tools": (args = {}) => call("describe_tools", args),
    },
    "debugger": {
      /**
     * Call a method on a live node in the running Godot game through the NIUA runtime probe (node.callv). Completes the runtime verification surface: read properties + write properties + inject input + call methods. The node must exist and expose the method; the return value is serialized back (Objects as class name + path only).
     * @param {object} [args]
     * @param {Array} [args.args] Positional arguments passed to node.callv. Plain JSON scalars, arrays, and objects pass through; typ
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.method Method to invoke on the runtime node, for example reset_score or take_damage. The node must expose i
     * @param {string} args.nodePath Runtime node path, for example /root/Player.
     * @param {number} [args.pollIntervalMsec] Delay between runtime method call polling requests. Defaults to 100.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {number} [args.timeoutMsec] How long the MCP client should poll for the debugger response. Defaults to 3000.
     */
      "call_runtime_node_method": (args = {}) => call("call_runtime_node_method", args),
      /**
     * Capture a PNG screenshot from the running Godot game through the NIUA runtime probe. Pass savePath to write the PNG to disk and keep large base64 payloads out of the tool result. Returns available=false when the runtime renderer cannot expose pixels, such as headless mode.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.pollIntervalMsec] Delay between runtime screenshot polling requests. Defaults to 100.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} [args.savePath] When set, decode the PNG payload and write it to this filesystem path instead of returning inline ba
     * @param {number} [args.timeoutMsec] How long the MCP client should poll for the runtime screenshot response. Defaults to 3000.
     */
      "capture_runtime_screenshot": (args = {}) => call("capture_runtime_screenshot", args),
      /**
     * Read Godot debugger-panel state: sessions, breakpoints, recent debugger events, and performance monitors.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "get_debugger_state": (args = {}) => call("get_debugger_state", args),
      /**
     * Read filtered runtime/debugger events captured by the NIUA Godot debugger probe without requesting a fresh runtime snapshot.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {Array} [args.kinds] Optional event kind filter, for example ['session_started', 'runtime_state'].
     * @param {number} [args.limit] Maximum runtime/debugger events to return. Defaults to 100 and is capped by the editor bridge.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {number} [args.sinceMsec] Only return events with timeMsec greater than this value. Defaults to -1, meaning no lower bound.
     */
      "get_runtime_events": (args = {}) => call("get_runtime_events", args),
      /**
     * Inspect runtime node properties from the running Godot game through the NIUA runtime probe. Pass properties: ["hp", "score"] to return only those properties instead of the full ~100-entry dump.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.nodePath] Runtime node path, for example /root/Player. Defaults to /root.
     * @param {number} [args.pollIntervalMsec] Delay between runtime property polling requests. Defaults to 100.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {Array} [args.properties] Optional property-name allowlist, for example ["hp", "score"]. When set, only matching properties ar
     * @param {number} [args.timeoutMsec] How long the MCP client should poll for the debugger response. Defaults to 3000.
     */
      "get_runtime_node_properties": (args = {}) => call("get_runtime_node_properties", args),
      /**
     * Read a FRESH runtime scene-tree snapshot from the running game: the call requests a snapshot and polls until the probe answers (pending: false, sessions[].runtimeState.kind == "snapshot"), so the returned tree is current truth rather than a cached earlier state. Pass maxDepth to keep large runtime trees shallow (truncated nodes report childrenTruncated) and pathFilter to serialize only the subtree rooted at a live node path.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.maxDepth] Maximum runtime tree depth to return. 0 or omitted means the full tree (up to the probe's internal c
     * @param {string} [args.pathFilter] Only serialize the runtime subtree rooted at this live node path, for example /root/Player. An unkno
     * @param {number} [args.pollIntervalMsec] Delay between snapshot result polling requests. Defaults to 100.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {number} [args.timeoutMsec] How long to poll for the freshly requested snapshot before returning. Defaults to 3000. On timeout t
     */
      "get_runtime_state": (args = {}) => call("get_runtime_state", args),
      /**
     * Enable the NIUA runtime probe as a Godot autoload for runtime inspection workflows.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.save] Save project.godot after enabling the runtime probe autoload. Defaults to true.
     */
      "install_runtime_probe": (args = {}) => call("install_runtime_probe", args),
      /**
     * Send a low-level Godot debugger message to editor debugger sessions through the public debugger API.
     * @param {object} [args]
     * @param {boolean} [args.activeOnly] Only send to active debugger sessions. Defaults to true.
     * @param {Array} [args.data] Optional debugger message data array forwarded to Godot. Defaults to [].
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.message Debugger message name passed to EditorDebuggerSession.send_message, for example niua_mcp:snapshot.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "send_debugger_message": (args = {}) => call("send_debugger_message", args),
      /**
     * Inject input into the running Godot game through the NIUA runtime probe: press or release input-map actions (for example move_forward, jump) with an optional timed hold, send raw key events (for games that check physical keycodes directly — restart keys, menu shortcuts), click mouse buttons at viewport positions, and feed relative mouse-look motion. Lets an agent script a playthrough and verify gameplay without a human.
     * @param {object} [args]
     * @param {Array} [args.actions] Input-map actions to press or release in the running game, for example [{ action: "move_forward", pr
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {number} [args.holdMs] When set, press the actions and keys, wait this many milliseconds, then release what was pressed (a 
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {Array} [args.keys] Raw keyboard events fed via Input.parse_input_event as InputEventKey, for games that check keys dire
     * @param {Array} [args.mouseButtons] Mouse button events fed via Input.parse_input_event as InputEventMouseButton. Pass position to click
     * @param {object} [args.mouseMotion] Relative mouse-look motion fed via Input.parse_input_event as an InputEventMouseMotion with relative
     * @param {number} [args.pollIntervalMsec] Delay between runtime input polling requests. Defaults to 100.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {number} [args.timeoutMsec] How long the MCP client should poll for the runtime input response. Defaults to 3000.
     */
      "send_runtime_input": (args = {}) => call("send_runtime_input", args),
      /**
     * Set or clear a Godot debugger breakpoint in a GDScript file.
     * @param {object} [args]
     * @param {boolean} [args.enabled] Whether the breakpoint should be enabled. Defaults to true.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} args.line Godot script line number for the breakpoint.
     * @param {string} args.path GDScript path under res://, for example res://player.gd.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "set_debugger_breakpoint": (args = {}) => call("set_debugger_breakpoint", args),
      /**
     * Set a live runtime node property in the running Godot game through the NIUA runtime probe.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.nodePath Runtime node path, for example /root/Player.
     * @param {number} [args.pollIntervalMsec] Delay between runtime property polling requests. Defaults to 100.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} args.property Runtime node property name to set, for example visible or name.
     * @param {number} [args.timeoutMsec] How long the MCP client should poll for the debugger response. Defaults to 3000.
     * @param {*} args.value JSON value to set. Typed values may use { type: 'Vector3', x, y, z }, { type: 'Color', r, g, b, a },
     */
      "set_runtime_node_property": (args = {}) => call("set_runtime_node_property", args),
      /**
     * Enable or disable a Godot debugger profiler on active editor debugger sessions.
     * @param {object} [args]
     * @param {Array} [args.data] Optional profiler toggle data array forwarded to Godot. Defaults to [].
     * @param {boolean} args.enabled Whether to enable or disable the debugger profiler.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} args.profiler Godot EngineProfiler name passed to EditorDebuggerSession.toggle_profiler, for example scripts or se
     */
      "toggle_debugger_profiler": (args = {}) => call("toggle_debugger_profiler", args),
    },
    "export": {
      /**
     * Diagnose whether local Godot export templates are installed for the MCP Godot version.
     * @param {object} [args]
     * @param {string} [args.projectRoot] Optional allowlisted Godot project root. When provided, diagnostics include project.godot and export
     */
      "diagnose_export_templates": (args = {}) => call("diagnose_export_templates", args),
      /**
     * Export an allowlisted Godot project through the local Godot CLI.
     * @param {object} [args]
     * @param {string} [args.mode] Export mode: release or debug. Defaults to release.
     * @param {string} args.outputPath Allowlisted output artifact path.
     * @param {string} args.preset Export preset name from export_presets.cfg.
     * @param {string} args.projectRoot Allowlisted Godot project root to export.
     * @param {number} [args.timeoutMs] Export process timeout in milliseconds. Defaults to 120000.
     */
      "export_project": (args = {}) => call("export_project", args),
      /**
     * List Godot export presets configured in export_presets.cfg.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "list_export_presets": (args = {}) => call("list_export_presets", args),
      /**
     * Create or update a Godot export preset through the visible editor bridge.
     * @param {object} [args]
     * @param {string} [args.customFeatures] Comma-separated custom export features. Defaults to empty.
     * @param {boolean} [args.dedicatedServer] Godot dedicated_server flag. Defaults to false.
     * @param {string} [args.excludeFilter] Godot exclude_filter value. Defaults to empty.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.exportFilter] Godot export_filter value. Defaults to all_resources.
     * @param {string} [args.exportPath] Default export path stored in export_presets.cfg.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.includeFilter] Godot include_filter value. Defaults to empty.
     * @param {number} [args.index] Optional preset index to update. When omitted, matches by name/platform or appends.
     * @param {string} args.name Export preset name.
     * @param {object} [args.options] Additional preset option keys written under preset.N.options.
     * @param {string} args.platform Godot export platform, for example Linux, Windows Desktop, Web, macOS, Android, or iOS.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.runnable] Whether the preset is runnable from the editor. Defaults to true.
     */
      "upsert_export_preset": (args = {}) => call("upsert_export_preset", args),
      /**
     * Validate Godot export preset configuration before running a local export.
     * @param {object} [args]
     * @param {string} [args.preset] Optional export preset name to validate. When omitted, all presets are validated.
     * @param {string} args.projectRoot Allowlisted Godot project root containing export_presets.cfg.
     */
      "validate_export_preset": (args = {}) => call("validate_export_preset", args),
    },
    "filesystem": {
      /**
     * Run ordered copy, move, and delete operations under the Godot project res:// filesystem.
     * @param {object} [args]
     * @param {boolean} [args.continueOnError] Continue executing later operations after a failure. Defaults to false.
     * @param {boolean} [args.dryRun] Validate and report planned operations without mutating files. Defaults to false.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {Array} args.operations Ordered filesystem operations to execute under res://.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "batch_filesystem_operations": (args = {}) => call("batch_filesystem_operations", args),
      /**
     * Copy a file or folder under the Godot project res:// filesystem.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} args.fromPath Existing file or folder path under res://.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {boolean} [args.overwrite] Allow replacing colliding files or merging into an existing destination directory. Defaults to false
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} args.toPath Destination Godot project path under res://.
     */
      "copy_filesystem_entry": (args = {}) => call("copy_filesystem_entry", args),
      /**
     * Create a folder under the Godot project res:// filesystem.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.path Godot project path under res://.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "create_folder": (args = {}) => call("create_folder", args),
      /**
     * Delete a file or empty folder under the Godot project res:// filesystem.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.path Godot project path under res://.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "delete_filesystem_entry": (args = {}) => call("delete_filesystem_entry", args),
      /**
     * Read visible Godot FileSystem dock selection, current path/current directory, and scan progress.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "get_filesystem_dock_state": (args = {}) => call("get_filesystem_dock_state", args),
      /**
     * List Godot FileSystem dock entries under a res:// path. Entries are sorted by name ascending within each directory (directories and files interleaved); recursive listings expand each subdirectory depth-first in that order.
     * @param {object} [args]
     * @param {Array} [args.exclude] Path substrings to exclude, such as ["addons", ".godot"].
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.maxDepth] Maximum directory depth for recursive listings. 0 or omitted means unlimited.
     * @param {string} [args.path] Godot project path to list. Defaults to res://.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.recursive] Whether to include nested descendants. Defaults to false.
     */
      "list_filesystem": (args = {}) => call("list_filesystem", args),
      /**
     * Move or rename a file or folder under the Godot project res:// filesystem.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} args.fromPath Existing Godot project path under res://.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} args.toPath Destination Godot project path under res://.
     */
      "move_filesystem_entry": (args = {}) => call("move_filesystem_entry", args),
      /**
     * Read a UTF-8 text file from the Godot project res:// filesystem. Optional lineStart (1-based) + lineCount return only that line range; totalLines is always reported.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.lineCount] Number of lines to return from lineStart. Omitted = through end of file.
     * @param {number} [args.lineStart] 1-based first line to return. Out-of-range fails naming totalLines.
     * @param {string} args.path Godot project path under res://.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "read_text_file": (args = {}) => call("read_text_file", args),
      /**
     * Write a base64-encoded binary file under the Godot project res:// filesystem.
     * @param {object} [args]
     * @param {string} args.contentBase64 Base64-encoded binary file content.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.path Godot project path under res://.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.refreshAfterWrite] Ask the Godot editor filesystem to refresh/import this path after writing. Defaults to true.
     */
      "write_binary_file": (args = {}) => call("write_binary_file", args),
      /**
     * Write a UTF-8 text file under the Godot project res:// filesystem.
     * @param {object} [args]
     * @param {string} args.content UTF-8 text content to write.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.path Godot project path under res://.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.refreshAfterWrite] Ask the Godot editor filesystem to refresh/import this path after writing. Defaults to true.
     */
      "write_text_file": (args = {}) => call("write_text_file", args),
    },
    "import": {
      /**
     * Diagnose Godot import sidecar health, generated target files, dependencies, and stale source metadata for an asset.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.path Godot project path under res://.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "get_import_diagnostics": (args = {}) => call("get_import_diagnostics", args),
      /**
     * Read recent Godot Import dock and EditorFileSystem import/reimport events.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {Array} [args.kinds] Optional event kind filter, for example ['resources_reimported', 'sources_changed'].
     * @param {number} [args.limit] Maximum import/reimport events to return. Defaults to 100 and is capped by the editor bridge.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {number} [args.sinceMsec] Only return events with timeMsec greater than this value. Defaults to -1, meaning no lower bound.
     */
      "get_import_events": (args = {}) => call("get_import_events", args),
      /**
     * Read Godot .import sidecar metadata for an imported asset.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.path Godot project path under res://.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "get_import_metadata": (args = {}) => call("get_import_metadata", args),
      /**
     * Run a local Godot CLI import pass for an allowlisted project when the visible editor Import dock cannot safely import new source assets.
     * @param {object} [args]
     * @param {string} args.projectRoot Allowlisted Godot project root to import with the local Godot CLI.
     * @param {number} [args.timeoutMs] Import process timeout in milliseconds. Defaults to 120000.
     */
      "import_project_assets": (args = {}) => call("import_project_assets", args),
      /**
     * List assets with Godot .import metadata under a res:// folder. Assets are listed in sorted name order per directory, depth-first.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.path] Godot project folder path to search for imported assets. Defaults to res://.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.recursive] Whether to include nested imported assets. Defaults to true.
     */
      "list_imported_assets": (args = {}) => call("list_imported_assets", args),
      /**
     * Ask the visible Godot editor to reimport one or more project assets.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {Array} args.paths Godot resource paths to reimport.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {number} [args.timeoutMs] Bridge request timeout in milliseconds for the reimport operation. Defaults to 120000.
     */
      "reimport_assets": (args = {}) => call("reimport_assets", args),
      /**
     * Update Godot import-dock options in an asset .import sidecar and optionally reimport the asset.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {object} args.options Import option keys and JSON-compatible values to write under the .import [params] section.
     * @param {string} args.path Godot asset path whose .import sidecar should be updated.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.reimport] Reimport the asset after saving import options. Defaults to false.
     */
      "set_import_options": (args = {}) => call("set_import_options", args),
      /**
     * Poll until a res:// asset is import-ready (metadata or listing), or timeout. Use after write/reimport to avoid import races.
     * @param {object} [args]
     * @param {string} [args.assetPath] Alias of path.
     * @param {string} [args.expectedProjectRoot]
     * @param {string} [args.host]
     * @param {string} [args.path] res:// asset path to wait for (e.g. res://assets/mesh.glb).
     * @param {number} [args.pollMs] Poll interval in ms. Default 400.
     * @param {number} [args.port]
     * @param {number} [args.timeoutMs] Max wait in ms. Default 30000.
     */
      "wait_for_imported_asset": (args = {}) => call("wait_for_imported_asset", args),
    },
    "inspector": {
      /**
     * Read inspector-style editable properties for a selected or addressed node.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.nodePath] Path under the edited scene root. Empty string means current editor selection.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {Array} [args.properties] Only return these property names. Omit for all editable properties.
     * @param {boolean} [args.verbose] Include full editor metadata (hints, usage flags, sections, revert values). Defaults to false: compa
     */
      "get_inspector_properties": (args = {}) => call("get_inspector_properties", args),
      /**
     * Set a property on a node in the current edited Godot scene.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.nodePath Path under the edited scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} args.property Godot property name.
     * @param {*} args.value JSON value or typed object such as { type: 'Vector3', x: 1, y: 2, z: 3 }.
     */
      "set_node_property": (args = {}) => call("set_node_property", args),
    },
    "localization": {
      /**
     * Create a locale CSV plus generated Translation resource, register it, and optionally activate the locale.
     * @param {object} [args]
     * @param {boolean} [args.activate] Set TranslationServer locale to this locale after creation.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.locale Translation locale, for example en or es.
     * @param {object} args.messages Source message keys mapped to translated text.
     * @param {boolean} [args.overwrite] Overwrite existing CSV/resource files. Defaults to false.
     * @param {string} args.path Output CSV path under res://, usually res://locales/en.csv.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.save] Persist project settings after registration. Defaults to true.
     * @param {string} [args.translationPath] Optional generated .translation resource path. Defaults beside the CSV.
     */
      "create_csv_translation": (args = {}) => call("create_csv_translation", args),
      /**
     * Read active locale, registered translation resources, loaded locales, and message counts. loadedLocales and translations are sorted by locale; registeredTranslations keeps project-settings order.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "get_localization_state": (args = {}) => call("get_localization_state", args),
      /**
     * Register a Translation resource path in Godot project localization settings.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {boolean} [args.loadNow] Load and add the translation to TranslationServer immediately. Defaults to true for .translation res
     * @param {string} args.path Translation resource path under res:// to register in project settings.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.save] Persist project settings after registration. Defaults to true.
     */
      "register_translation_file": (args = {}) => call("register_translation_file", args),
      /**
     * Set Godot's active TranslationServer locale.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.locale Locale to activate through TranslationServer.set_locale.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "set_locale": (args = {}) => call("set_locale", args),
    },
    "multiplayer": {
      /**
     * Generate and attach a minimal ENet host/join script for localhost multiplayer probes.
     * @param {object} [args]
     * @param {number} [args.defaultPort]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.hostValue]
     * @param {string} args.nodePath Node that receives the generated ENet host/join script. Empty string means scene root.
     * @param {boolean} [args.overwrite]
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} args.propertyName
     * @param {string} args.scriptPath
     * @param {string} args.statePath NodePath from the scripted node to the synchronized state node.
     */
      "create_enet_multiplayer_script": (args = {}) => call("create_enet_multiplayer_script", args),
      /**
     * Create a MultiplayerSpawner with spawn path, limit, and optional spawnable scenes.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.name]
     * @param {string} [args.parentPath]
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {number} [args.spawnLimit]
     * @param {string} [args.spawnPath]
     * @param {Array} [args.spawnableScenes]
     */
      "create_multiplayer_spawner": (args = {}) => call("create_multiplayer_spawner", args),
      /**
     * Generate and attach a small Node script exposing one synchronized string property.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.initialValue]
     * @param {string} args.nodePath
     * @param {boolean} [args.overwrite]
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} args.propertyName
     * @param {string} args.scriptPath
     */
      "create_multiplayer_state_script": (args = {}) => call("create_multiplayer_state_script", args),
      /**
     * Create a MultiplayerSynchronizer with a SceneReplicationConfig for selected property paths.
     * @param {object} [args]
     * @param {number} [args.deltaInterval]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.name]
     * @param {string} args.parentPath
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {Array} args.propertyPaths
     * @param {boolean} [args.publicVisibility]
     * @param {number} [args.replicationInterval]
     * @param {string} [args.rootPath]
     */
      "create_multiplayer_synchronizer": (args = {}) => call("create_multiplayer_synchronizer", args),
    },
    "navigation": {
      /**
     * Bake the NavigationMesh for a NavigationRegion3D from scene geometry.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {boolean} [args.onThread] Run Godot's bake on a thread. Defaults to false for deterministic probe behavior.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} args.regionPath NavigationRegion3D path under the edited scene root.
     * @param {number} [args.timeoutMs] Bridge request timeout in milliseconds for the bake operation. Defaults to 120000.
     */
      "bake_navigation_mesh_3d": (args = {}) => call("bake_navigation_mesh_3d", args),
      /**
     * Create a NavigationAgent3D under an actor node with practical path-following settings.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {number} [args.height]
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.maxSpeed]
     * @param {string} [args.name]
     * @param {string} args.parentPath Parent node path under the edited scene root.
     * @param {number} [args.pathDesiredDistance]
     * @param {number} [args.pathMaxDistance]
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {number} [args.radius]
     * @param {number} [args.targetDesiredDistance]
     * @param {object} [args.targetPosition]
     */
      "create_navigation_agent_3d": (args = {}) => call("create_navigation_agent_3d", args),
      /**
     * Create a NavigationRegion3D with a NavigationMesh resource and practical agent/bake settings.
     * @param {object} [args]
     * @param {number} [args.agentHeight]
     * @param {number} [args.agentMaxClimb]
     * @param {number} [args.agentMaxSlope]
     * @param {number} [args.agentRadius]
     * @param {number} [args.cellHeight]
     * @param {number} [args.cellSize]
     * @param {boolean} [args.enabled]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.name]
     * @param {string} [args.parentPath] Parent path under the edited scene root. Empty string means the scene root.
     * @param {number} [args.parsedGeometryType] Raw NavigationMesh parsed_geometry_type enum value for advanced workflows.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {number} [args.sourceGeometryMode] Raw NavigationMesh geometry_source_geometry_mode enum value for advanced workflows.
     */
      "create_navigation_region_3d": (args = {}) => call("create_navigation_region_3d", args),
      /**
     * Generate and attach a CharacterBody3D script template that follows a target through a NavigationAgent3D.
     * @param {object} [args]
     * @param {string} [args.agentPath] NavigationAgent3D path relative to nodePath. Defaults to NavigationAgent3D.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.nodePath CharacterBody3D node path that will receive the generated script.
     * @param {boolean} [args.overwrite]
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} args.scriptPath Output script path under res://.
     * @param {number} [args.speed]
     * @param {string} [args.targetPath] Target Node3D path relative to nodePath. Defaults to ../Target.
     */
      "create_navigation_target_follow_script": (args = {}) => call("create_navigation_target_follow_script", args),
    },
    "nodes-2d": {
      /**
     * Create an AnimatedSprite2D with an existing SpriteFrames resource or a newly generated SpriteFrames asset.
     * @param {object} [args]
     * @param {string} [args.animation] Initial AnimatedSprite2D animation name.
     * @param {Array} [args.animations] Named SpriteFrames animations with Texture2D frames or sprite-sheet grids.
     * @param {string} [args.autoplay] Animation name to autoplay.
     * @param {boolean} [args.centered] Whether the sprite is centered.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {boolean} [args.flipH] Whether the sprite is horizontally flipped.
     * @param {boolean} [args.flipV] Whether the sprite is vertically flipped.
     * @param {number} [args.frame] Initial frame index.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.name] Optional AnimatedSprite2D node name.
     * @param {*} [args.offset] AnimatedSprite2D offset as [x,y] or { x, y }.
     * @param {boolean} [args.openSpriteFrames] Open generated SpriteFrames in the visible editor. Defaults to false.
     * @param {boolean} [args.overwriteSpriteFrames] Overwrite generated SpriteFrames. Defaults to false.
     * @param {string} [args.parentPath] Path under the edited scene root. Empty string means the scene root.
     * @param {boolean} [args.playing] Whether the animation starts playing.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {*} [args.position] Optional Node2D position as [x,y] or { x, y }.
     * @param {object} [args.properties] Advanced Godot node properties merged after curated fields.
     * @param {string} [args.resourceName] Optional resource_name for generated SpriteFrames.
     * @param {number} [args.rotationDegrees] Optional Node2D rotation_degrees value.
     * @param {*} [args.scale] Optional Node2D scale as [x,y] or { x, y }.
     * @param {number} [args.speedScale] AnimatedSprite2D speed_scale.
     * @param {string} [args.spriteFramesPath] Existing SpriteFrames resource path under res://.
     * @param {string} [args.spriteFramesResourcePath] Generated SpriteFrames output path when spriteFramesPath is omitted.
     */
      "create_animated_sprite_2d": (args = {}) => call("create_animated_sprite_2d", args),
      /**
     * Create an Area2D trigger or sensor and optionally create a Shape2D resource, CollisionShape2D child, and visible Sprite2D helper.
     * @param {object} [args]
     * @param {boolean} [args.collisionDisabled] Whether the CollisionShape2D child starts disabled.
     * @param {number} [args.collisionHeight] CapsuleShape2D height.
     * @param {number} [args.collisionLayer] Physics collision_layer integer.
     * @param {number} [args.collisionMask] Physics collision_mask integer.
     * @param {string} [args.collisionName] Name for the CollisionShape2D child.
     * @param {object} [args.collisionNodeProperties] Advanced Godot CollisionShape2D properties merged after curated fields.
     * @param {*} [args.collisionPosition] Optional CollisionShape2D local position as [x,y] or { x, y }.
     * @param {number} [args.collisionRadius] CircleShape2D or CapsuleShape2D radius.
     * @param {number} [args.collisionRotationDegrees] Optional CollisionShape2D rotation_degrees value.
     * @param {*} [args.collisionScale] Optional CollisionShape2D local scale as [x,y] or { x, y }.
     * @param {string} [args.collisionShapeKind] Shape resource kind: rectangle, circle, or capsule. Defaults to rectangle.
     * @param {string} [args.collisionShapePath] Godot Shape2D resource output path under res://, usually ending in .tres.
     * @param {object} [args.collisionShapeProperties] Advanced Godot Shape2D properties merged after curated fields.
     * @param {*} [args.collisionSize] RectangleShape2D size as [x,y] or { x, y }.
     * @param {boolean} [args.createVisual] Create a visible Sprite2D child after the collision child. Defaults to false.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {boolean} [args.monitorable] Whether other areas can monitor this Area2D.
     * @param {boolean} [args.monitoring] Whether Area2D monitoring is enabled.
     * @param {string} [args.name] Optional Area2D node name.
     * @param {boolean} [args.openCollisionShape] Open the shape resource in the editor after creation. Defaults to false.
     * @param {boolean} [args.overwriteCollisionShape] Overwrite an existing shape resource. Defaults to false.
     * @param {boolean} [args.overwriteVisualTexture] Overwrite the optional visual placeholder texture resource. Defaults to false.
     * @param {string} [args.parentPath] Path under the edited scene root. Empty string means the scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {*} [args.position] Optional Node2D position as [x,y] or { x, y }.
     * @param {number} [args.priority] Area2D priority.
     * @param {object} [args.properties] Advanced Godot node properties merged after curated fields.
     * @param {number} [args.rotationDegrees] Optional Node2D rotation_degrees value.
     * @param {*} [args.scale] Optional Node2D scale as [x,y] or { x, y }.
     * @param {string} [args.visualName] Name for the optional visible Sprite2D child.
     * @param {string} [args.visualPlaceholderTexturePath] Generated PlaceholderTexture2D output path for the optional Sprite2D child.
     * @param {*} [args.visualPosition] Optional Sprite2D local position as [x,y] or { x, y }.
     * @param {object} [args.visualProperties] Advanced Godot Sprite2D properties merged after curated fields.
     * @param {number} [args.visualRotationDegrees] Optional Sprite2D local rotation_degrees value.
     * @param {*} [args.visualScale] Optional Sprite2D local scale as [x,y] or { x, y }.
     * @param {*} [args.visualSize] PlaceholderTexture2D size as [x,y] or { x, y }.
     * @param {string} [args.visualTexturePath] Existing texture resource path assigned to the optional Sprite2D child.
     */
      "create_area_2d": (args = {}) => call("create_area_2d", args),
      /**
     * Create a Camera2D with practical position, zoom, enabled state, and limit fields.
     * @param {object} [args]
     * @param {boolean} [args.enabled] Whether this Camera2D starts enabled.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.limitBottom] Camera2D limit_bottom value.
     * @param {number} [args.limitLeft] Camera2D limit_left value.
     * @param {number} [args.limitRight] Camera2D limit_right value.
     * @param {number} [args.limitTop] Camera2D limit_top value.
     * @param {string} [args.name] Optional Camera2D node name.
     * @param {string} [args.parentPath] Path under the edited scene root. Empty string means the scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {*} [args.position] Optional Node2D position as [x,y] or { x, y }.
     * @param {object} [args.properties] Advanced Godot node properties merged after curated fields.
     * @param {number} [args.rotationDegrees] Optional Node2D rotation_degrees value.
     * @param {*} [args.scale] Optional Node2D scale as [x,y] or { x, y }.
     * @param {*} [args.zoom] Camera2D zoom as [x,y] or { x, y }.
     */
      "create_camera_2d": (args = {}) => call("create_camera_2d", args),
      /**
     * Create a CharacterBody2D with collision and an optional visible Sprite2D child.
     * @param {object} [args]
     * @param {boolean} [args.collisionDisabled] Whether the CollisionShape2D child starts disabled.
     * @param {number} [args.collisionHeight] CapsuleShape2D height.
     * @param {number} [args.collisionLayer] Physics collision_layer integer.
     * @param {number} [args.collisionMask] Physics collision_mask integer.
     * @param {string} [args.collisionName] Name for the CollisionShape2D child.
     * @param {object} [args.collisionNodeProperties] Advanced Godot CollisionShape2D properties merged after curated fields.
     * @param {*} [args.collisionPosition] Optional CollisionShape2D local position as [x,y] or { x, y }.
     * @param {number} [args.collisionRadius] CircleShape2D or CapsuleShape2D radius.
     * @param {number} [args.collisionRotationDegrees] Optional CollisionShape2D rotation_degrees value.
     * @param {*} [args.collisionScale] Optional CollisionShape2D local scale as [x,y] or { x, y }.
     * @param {string} [args.collisionShapeKind] Shape resource kind: rectangle, circle, or capsule. Defaults to rectangle.
     * @param {string} args.collisionShapePath Godot Shape2D resource output path under res://, usually ending in .tres.
     * @param {object} [args.collisionShapeProperties] Advanced Godot Shape2D properties merged after curated fields.
     * @param {*} [args.collisionSize] RectangleShape2D size as [x,y] or { x, y }.
     * @param {boolean} [args.createVisual] Create a visible Sprite2D child after the collision child. Defaults to false.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.name] Optional CharacterBody2D node name.
     * @param {boolean} [args.openCollisionShape] Open the shape resource in the editor after creation. Defaults to false.
     * @param {boolean} [args.overwriteCollisionShape] Overwrite an existing shape resource. Defaults to false.
     * @param {boolean} [args.overwriteVisualTexture] Overwrite the optional visual placeholder texture resource. Defaults to false.
     * @param {string} [args.parentPath] Path under the edited scene root. Empty string means the scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {*} [args.position] Optional Node2D position as [x,y] or { x, y }.
     * @param {object} [args.properties] Advanced Godot node properties merged after curated fields.
     * @param {number} [args.rotationDegrees] Optional Node2D rotation_degrees value.
     * @param {*} [args.scale] Optional Node2D scale as [x,y] or { x, y }.
     * @param {string} [args.visualName] Name for the optional visible Sprite2D child.
     * @param {string} [args.visualPlaceholderTexturePath] Generated PlaceholderTexture2D output path for the optional Sprite2D child.
     * @param {*} [args.visualPosition] Optional Sprite2D local position as [x,y] or { x, y }.
     * @param {object} [args.visualProperties] Advanced Godot Sprite2D properties merged after curated fields.
     * @param {number} [args.visualRotationDegrees] Optional Sprite2D local rotation_degrees value.
     * @param {*} [args.visualScale] Optional Sprite2D local scale as [x,y] or { x, y }.
     * @param {*} [args.visualSize] PlaceholderTexture2D size as [x,y] or { x, y }.
     * @param {string} [args.visualTexturePath] Existing texture resource path assigned to the optional Sprite2D child.
     */
      "create_character_body_2d": (args = {}) => call("create_character_body_2d", args),
      /**
     * Create a Shape2D resource and CollisionShape2D node for 2D physics workflows.
     * @param {object} [args]
     * @param {boolean} [args.disabled] Whether the CollisionShape2D node starts disabled.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {number} [args.height] CapsuleShape2D height.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.name] Optional CollisionShape2D node name.
     * @param {object} [args.nodeProperties] Advanced Godot CollisionShape2D properties merged after curated fields.
     * @param {boolean} [args.open] Open the shape resource after creation. Defaults to false.
     * @param {boolean} [args.overwrite] Overwrite an existing shape resource. Defaults to false.
     * @param {string} [args.parentPath] Path under the edited scene root. Empty string means the scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {*} [args.position] Optional Node2D position as [x,y] or { x, y }.
     * @param {number} [args.radius] CircleShape2D or CapsuleShape2D radius.
     * @param {number} [args.rotationDegrees] Optional Node2D rotation_degrees value.
     * @param {*} [args.scale] Optional Node2D scale as [x,y] or { x, y }.
     * @param {string} [args.shapeKind] Shape resource kind: rectangle, circle, or capsule. Defaults to rectangle.
     * @param {string} args.shapePath Godot Shape2D resource output path under res://, usually ending in .tres.
     * @param {object} [args.shapeProperties] Advanced Godot Shape2D properties merged after curated fields.
     * @param {*} [args.size] RectangleShape2D size as [x,y] or { x, y }. Defaults to [64,64].
     */
      "create_collision_shape_2d": (args = {}) => call("create_collision_shape_2d", args),
      /**
     * Create a Sprite2D with an imported texture or generated PlaceholderTexture2D for visible 2D blockouts.
     * @param {object} [args]
     * @param {boolean} [args.createPlaceholderTexture] Create a PlaceholderTexture2D when texturePath is not provided. Defaults to true.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.name] Optional Sprite2D node name.
     * @param {boolean} [args.openTexture] Open the generated placeholder texture resource. Defaults to false.
     * @param {boolean} [args.overwriteTexture] Overwrite the generated placeholder texture resource. Defaults to false.
     * @param {string} [args.parentPath] Path under the edited scene root. Empty string means the scene root.
     * @param {string} [args.placeholderTexturePath] Generated PlaceholderTexture2D output path.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {*} [args.position] Optional Node2D position as [x,y] or { x, y }.
     * @param {object} [args.properties] Advanced Godot node properties merged after curated fields.
     * @param {number} [args.rotationDegrees] Optional Node2D rotation_degrees value.
     * @param {*} [args.scale] Optional Node2D scale as [x,y] or { x, y }.
     * @param {*} [args.size] PlaceholderTexture2D size as [x,y] or { x, y }. Defaults to [64,64].
     * @param {string} [args.texturePath] Existing texture resource path assigned to Sprite2D.texture.
     */
      "create_sprite_2d": (args = {}) => call("create_sprite_2d", args),
      /**
     * Create a StaticBody2D with collision and an optional visible Sprite2D child.
     * @param {object} [args]
     * @param {boolean} [args.collisionDisabled] Whether the CollisionShape2D child starts disabled.
     * @param {number} [args.collisionHeight] CapsuleShape2D height.
     * @param {number} [args.collisionLayer] Physics collision_layer integer.
     * @param {number} [args.collisionMask] Physics collision_mask integer.
     * @param {string} [args.collisionName] Name for the CollisionShape2D child.
     * @param {object} [args.collisionNodeProperties] Advanced Godot CollisionShape2D properties merged after curated fields.
     * @param {*} [args.collisionPosition] Optional CollisionShape2D local position as [x,y] or { x, y }.
     * @param {number} [args.collisionRadius] CircleShape2D or CapsuleShape2D radius.
     * @param {number} [args.collisionRotationDegrees] Optional CollisionShape2D rotation_degrees value.
     * @param {*} [args.collisionScale] Optional CollisionShape2D local scale as [x,y] or { x, y }.
     * @param {string} [args.collisionShapeKind] Shape resource kind: rectangle, circle, or capsule. Defaults to rectangle.
     * @param {string} args.collisionShapePath Godot Shape2D resource output path under res://, usually ending in .tres.
     * @param {object} [args.collisionShapeProperties] Advanced Godot Shape2D properties merged after curated fields.
     * @param {*} [args.collisionSize] RectangleShape2D size as [x,y] or { x, y }.
     * @param {boolean} [args.createVisual] Create a visible Sprite2D child after the collision child. Defaults to false.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.name] Optional StaticBody2D node name.
     * @param {boolean} [args.openCollisionShape] Open the shape resource in the editor after creation. Defaults to false.
     * @param {boolean} [args.overwriteCollisionShape] Overwrite an existing shape resource. Defaults to false.
     * @param {boolean} [args.overwriteVisualTexture] Overwrite the optional visual placeholder texture resource. Defaults to false.
     * @param {string} [args.parentPath] Path under the edited scene root. Empty string means the scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {*} [args.position] Optional Node2D position as [x,y] or { x, y }.
     * @param {object} [args.properties] Advanced Godot node properties merged after curated fields.
     * @param {number} [args.rotationDegrees] Optional Node2D rotation_degrees value.
     * @param {*} [args.scale] Optional Node2D scale as [x,y] or { x, y }.
     * @param {string} [args.visualName] Name for the optional visible Sprite2D child.
     * @param {string} [args.visualPlaceholderTexturePath] Generated PlaceholderTexture2D output path for the optional Sprite2D child.
     * @param {*} [args.visualPosition] Optional Sprite2D local position as [x,y] or { x, y }.
     * @param {object} [args.visualProperties] Advanced Godot Sprite2D properties merged after curated fields.
     * @param {number} [args.visualRotationDegrees] Optional Sprite2D local rotation_degrees value.
     * @param {*} [args.visualScale] Optional Sprite2D local scale as [x,y] or { x, y }.
     * @param {*} [args.visualSize] PlaceholderTexture2D size as [x,y] or { x, y }.
     * @param {string} [args.visualTexturePath] Existing texture resource path assigned to the optional Sprite2D child.
     */
      "create_static_body_2d": (args = {}) => call("create_static_body_2d", args),
      /**
     * Create a TileMapLayer with an existing or generated TileSet and optional initial cell painting.
     * @param {object} [args]
     * @param {Array} [args.cells] TileMapLayer cell set/erase operations.
     * @param {boolean} [args.clearCells] Clear existing used cells before applying cells. Defaults to false.
     * @param {boolean} [args.collisionEnabled] TileMapLayer collision_enabled state.
     * @param {boolean} [args.enabled] TileMapLayer enabled state.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.name] Optional TileMapLayer node name.
     * @param {boolean} [args.navigationEnabled] TileMapLayer navigation_enabled state.
     * @param {boolean} [args.openTileSet] Open generated TileSet in the visible editor. Defaults to false.
     * @param {boolean} [args.overwriteTileSet] Overwrite generated TileSet. Defaults to false.
     * @param {string} [args.parentPath] Path under the edited scene root. Empty string means the scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {*} [args.position] Optional Node2D position as [x,y] or { x, y }.
     * @param {object} [args.properties] Advanced Godot node properties merged after curated fields.
     * @param {number} [args.renderingQuadrantSize] TileMapLayer rendering_quadrant_size.
     * @param {string} [args.resourceName] Optional Godot resource_name for the TileSet asset.
     * @param {number} [args.rotationDegrees] Optional Node2D rotation_degrees value.
     * @param {*} [args.scale] Optional Node2D scale as [x,y] or { x, y }.
     * @param {Array} [args.sources] TileSetAtlasSource entries backed by existing Texture2D atlases.
     * @param {string} [args.tileSetPath] Existing TileSet resource path under res://.
     * @param {string} [args.tileSetResourcePath] Generated TileSet output path when tileSetPath is omitted.
     * @param {*} [args.tileSize] TileSet tile size. Defaults to [16,16].
     */
      "create_tile_map_layer": (args = {}) => call("create_tile_map_layer", args),
      /**
     * Paint terrain-aware cells on an existing TileMapLayer using Godot's terrain connect or path solver.
     * @param {object} [args]
     * @param {Array} args.coords TileMapLayer map coordinates as [x,y] or { x, y } integers.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {boolean} [args.ignoreEmptyTerrains] Forwarded to Godot's terrain solver. Defaults to true.
     * @param {string} [args.mode] Terrain painting mode. connect updates connected terrain regions; path connects successive coordinat
     * @param {string} args.nodePath Existing TileMapLayer node path under the edited scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {number} args.terrain Terrain index within the terrain set.
     * @param {number} args.terrainSet TileSet terrain set index.
     */
      "paint_tile_map_layer_terrain": (args = {}) => call("paint_tile_map_layer_terrain", args),
      /**
     * Set or erase cells on an existing TileMapLayer using TileSet source ids and atlas coordinates.
     * @param {object} [args]
     * @param {Array} [args.cells] TileMapLayer cell set/erase operations.
     * @param {boolean} [args.clear] Clear existing used cells before applying cells. Defaults to false.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.nodePath Existing TileMapLayer node path under the edited scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "set_tile_map_layer_cells": (args = {}) => call("set_tile_map_layer_cells", args),
    },
    "nodes-3d": {
      /**
     * Create an Area3D trigger/sensor volume and optionally create a Shape3D resource plus CollisionShape3D child.
     * @param {object} [args]
     * @param {boolean} [args.collisionDisabled] Whether the CollisionShape3D child starts disabled.
     * @param {number} [args.collisionHeight] Capsule or cylinder collision height.
     * @param {number} [args.collisionLayer] CollisionObject3D collision_layer bitmask.
     * @param {number} [args.collisionMargin] Optional collision Shape3D margin.
     * @param {number} [args.collisionMask] CollisionObject3D collision_mask bitmask.
     * @param {string} [args.collisionName] Optional CollisionShape3D child name.
     * @param {object} [args.collisionNodeProperties] Advanced Godot CollisionShape3D child properties merged after curated collision fields.
     * @param {*} [args.collisionPosition] Optional child CollisionShape3D position as [x,y,z] or { x, y, z }.
     * @param {number} [args.collisionRadius] Sphere, capsule, or cylinder collision radius.
     * @param {*} [args.collisionRotationDegrees] Optional child CollisionShape3D rotation_degrees as [x,y,z] or { x, y, z }.
     * @param {*} [args.collisionScale] Optional child CollisionShape3D scale as [x,y,z] or { x, y, z }.
     * @param {string} [args.collisionShapeKind] Optional child collision shape resource kind: box, sphere, capsule, or cylinder. Defaults to box whe
     * @param {string} [args.collisionShapePath] Optional Shape3D resource path to create and attach as a CollisionShape3D child.
     * @param {object} [args.collisionShapeProperties] Advanced Godot Shape3D properties merged after curated collision fields.
     * @param {*} [args.collisionSize] BoxShape3D child size as [x,y,z] or { x, y, z }.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {boolean} [args.monitorable] Whether the Area3D is monitorable by other areas.
     * @param {boolean} [args.monitoring] Whether Area3D monitoring is enabled.
     * @param {string} [args.name] Optional Area3D node name.
     * @param {boolean} [args.openCollisionShape] Open the collision shape resource in the visible editor after creation. Defaults to false.
     * @param {boolean} [args.overwriteCollisionShape] Overwrite an existing collision shape resource. Defaults to false.
     * @param {string} [args.parentPath] Path under the edited scene root. Empty string means the scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {*} [args.position] Optional Node3D position as [x,y,z] or { x, y, z }.
     * @param {number} [args.priority] Area3D priority.
     * @param {object} [args.properties] Advanced Godot Area3D node properties merged after curated fields.
     * @param {*} [args.rotationDegrees] Optional Node3D rotation_degrees as [x,y,z] or { x, y, z }.
     * @param {*} [args.scale] Optional Node3D scale as [x,y,z] or { x, y, z }.
     */
      "create_area_3d": (args = {}) => call("create_area_3d", args),
      /**
     * Create a Camera3D with practical transform, projection, FOV, and clipping fields.
     * @param {object} [args]
     * @param {boolean} [args.current] Whether this Camera3D should be the current scene camera.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {number} [args.far] Camera3D far clipping distance.
     * @param {number} [args.fov] Camera3D field of view in degrees for perspective projection.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.name] Optional camera node name.
     * @param {number} [args.near] Camera3D near clipping distance.
     * @param {string} [args.parentPath] Path under the edited scene root. Empty string means the scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {*} [args.position] Optional Node3D position as [x,y,z] or { x, y, z }.
     * @param {*} [args.projection] Projection mode: perspective, orthogonal, frustum, or the Godot enum integer.
     * @param {object} [args.properties] Advanced Godot Camera3D properties merged after curated fields.
     * @param {*} [args.rotationDegrees] Optional Node3D rotation_degrees as [x,y,z] or { x, y, z }.
     * @param {*} [args.scale] Optional Node3D scale as [x,y,z] or { x, y, z }.
     * @param {number} [args.size] Camera3D orthogonal size.
     */
      "create_camera_3d": (args = {}) => call("create_camera_3d", args),
      /**
     * Create a CharacterBody3D controller body and optionally create a Shape3D resource plus CollisionShape3D child.
     * @param {object} [args]
     * @param {boolean} [args.collisionDisabled] Whether the CollisionShape3D child starts disabled.
     * @param {number} [args.collisionHeight] Capsule or cylinder collision height.
     * @param {number} [args.collisionLayer] CollisionObject3D collision_layer bitmask.
     * @param {number} [args.collisionMargin] Optional collision Shape3D margin.
     * @param {number} [args.collisionMask] CollisionObject3D collision_mask bitmask.
     * @param {string} [args.collisionName] Optional CollisionShape3D child name.
     * @param {object} [args.collisionNodeProperties] Advanced Godot CollisionShape3D child properties merged after curated collision fields.
     * @param {*} [args.collisionPosition] Optional child CollisionShape3D position as [x,y,z] or { x, y, z }.
     * @param {number} [args.collisionRadius] Sphere, capsule, or cylinder collision radius.
     * @param {*} [args.collisionRotationDegrees] Optional child CollisionShape3D rotation_degrees as [x,y,z] or { x, y, z }.
     * @param {*} [args.collisionScale] Optional child CollisionShape3D scale as [x,y,z] or { x, y, z }.
     * @param {string} [args.collisionShapeKind] Optional child collision shape resource kind: box, sphere, capsule, or cylinder. Defaults to box whe
     * @param {string} [args.collisionShapePath] Optional Shape3D resource path to create and attach as a CollisionShape3D child.
     * @param {object} [args.collisionShapeProperties] Advanced Godot Shape3D properties merged after curated collision fields.
     * @param {*} [args.collisionSize] BoxShape3D child size as [x,y,z] or { x, y, z }.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {boolean} [args.floorBlockOnWall] CharacterBody3D floor_block_on_wall.
     * @param {boolean} [args.floorConstantSpeed] CharacterBody3D floor_constant_speed.
     * @param {number} [args.floorMaxAngle] CharacterBody3D floor_max_angle.
     * @param {number} [args.floorSnapLength] CharacterBody3D floor_snap_length.
     * @param {boolean} [args.floorStopOnSlope] CharacterBody3D floor_stop_on_slope.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {*} [args.motionMode] CharacterBody3D motion_mode: grounded, floating, or the Godot enum integer.
     * @param {string} [args.name] Optional CharacterBody3D node name.
     * @param {boolean} [args.openCollisionShape] Open the collision shape resource in the visible editor after creation. Defaults to false.
     * @param {boolean} [args.overwriteCollisionShape] Overwrite an existing collision shape resource. Defaults to false.
     * @param {string} [args.parentPath] Path under the edited scene root. Empty string means the scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {*} [args.position] Optional Node3D position as [x,y,z] or { x, y, z }.
     * @param {object} [args.properties] Advanced Godot CharacterBody3D node properties merged after curated fields.
     * @param {*} [args.rotationDegrees] Optional Node3D rotation_degrees as [x,y,z] or { x, y, z }.
     * @param {*} [args.scale] Optional Node3D scale as [x,y,z] or { x, y, z }.
     * @param {boolean} [args.slideOnCeiling] CharacterBody3D slide_on_ceiling.
     * @param {*} [args.upDirection] CharacterBody3D up_direction as [x,y,z] or { x, y, z }.
     * @param {*} [args.velocity] Initial CharacterBody3D velocity as [x,y,z] or { x, y, z }.
     * @param {number} [args.wallMinSlideAngle] CharacterBody3D wall_min_slide_angle.
     */
      "create_character_body_3d": (args = {}) => call("create_character_body_3d", args),
      /**
     * Create a Shape3D resource and a CollisionShape3D node that references it for 3D physics workflows.
     * @param {object} [args]
     * @param {boolean} [args.disabled] Whether the CollisionShape3D node starts disabled.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {number} [args.height] Capsule or cylinder height.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.margin] Optional Shape3D margin.
     * @param {string} [args.name] Optional CollisionShape3D node name.
     * @param {object} [args.nodeProperties] Advanced Godot CollisionShape3D node properties merged after curated fields.
     * @param {boolean} [args.open] Open the shape resource in the visible editor after creation. Defaults to false.
     * @param {boolean} [args.overwrite] Overwrite an existing shape resource. Defaults to false.
     * @param {string} [args.parentPath] Path under the edited scene root. Empty string means the scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {*} [args.position] Optional Node3D position as [x,y,z] or { x, y, z }.
     * @param {number} [args.radius] Sphere, capsule, or cylinder radius.
     * @param {*} [args.rotationDegrees] Optional Node3D rotation_degrees as [x,y,z] or { x, y, z }.
     * @param {*} [args.scale] Optional Node3D scale as [x,y,z] or { x, y, z }.
     * @param {string} [args.shapeKind] Shape resource kind: box, sphere, capsule, or cylinder. Defaults to box.
     * @param {string} args.shapePath Godot Shape3D resource output path under res://, usually ending in .tres.
     * @param {object} [args.shapeProperties] Advanced Godot Shape3D properties merged after curated fields.
     * @param {*} [args.size] BoxShape3D size as [x,y,z] or { x, y, z }.
     */
      "create_collision_shape_3d": (args = {}) => call("create_collision_shape_3d", args),
      /**
     * Create a DirectionalLight3D, OmniLight3D, or SpotLight3D with practical transform and lighting fields.
     * @param {object} [args]
     * @param {number} [args.angleDegrees] SpotLight3D spot_angle value.
     * @param {*} [args.color] Light color as #RRGGBB, #RRGGBBAA, or { r, g, b, a } values in 0..1.
     * @param {number} [args.energy] Light3D light_energy value.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.kind] Light kind to create: directional, omni, point, or spot. Defaults to omni.
     * @param {string} [args.name] Optional light node name.
     * @param {string} [args.parentPath] Path under the edited scene root. Empty string means the scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {*} [args.position] Optional Node3D position as [x,y,z] or { x, y, z }.
     * @param {object} [args.properties] Advanced Godot light node properties merged after curated fields.
     * @param {number} [args.range] OmniLight3D omni_range or SpotLight3D spot_range value.
     * @param {*} [args.rotationDegrees] Optional Node3D rotation_degrees as [x,y,z] or { x, y, z }.
     * @param {*} [args.scale] Optional Node3D scale as [x,y,z] or { x, y, z }.
     * @param {boolean} [args.shadowEnabled] Whether shadows are enabled for the light.
     */
      "create_light_3d": (args = {}) => call("create_light_3d", args),
      /**
     * Create a primitive Mesh resource and a MeshInstance3D node that references it for visible 3D blockouts.
     * @param {object} [args]
     * @param {number} [args.bottomRadius] Cylinder bottom_radius override.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {number} [args.height] Sphere, Capsule, or Cylinder height.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.innerRadius] Torus inner_radius.
     * @param {string} [args.materialPath] Optional res:// Material resource assigned to GeometryInstance3D material_override.
     * @param {string} [args.meshKind] Primitive mesh resource kind: box, cube, sphere, capsule, cylinder, plane, quad, or torus. Defaults 
     * @param {string} args.meshPath Godot Mesh resource output path under res://, usually ending in .tres.
     * @param {object} [args.meshProperties] Advanced Godot primitive mesh properties merged after curated fields.
     * @param {string} [args.name] Optional MeshInstance3D node name.
     * @param {object} [args.nodeProperties] Advanced Godot MeshInstance3D node properties merged after curated fields.
     * @param {boolean} [args.open] Open the mesh resource in the visible editor after creation. Defaults to false.
     * @param {number} [args.outerRadius] Torus outer_radius.
     * @param {boolean} [args.overwrite] Overwrite an existing mesh resource. Defaults to false.
     * @param {string} [args.parentPath] Path under the edited scene root. Empty string means the scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {*} [args.position] Optional Node3D position as [x,y,z] or { x, y, z }.
     * @param {number} [args.radialSegments] Primitive radial segment count where the mesh type supports it.
     * @param {number} [args.radius] Sphere/Capsule radius, or shared Cylinder top/bottom radius.
     * @param {number} [args.ringSegments] Torus ring_segments value.
     * @param {number} [args.rings] Primitive ring count where the mesh type supports it.
     * @param {*} [args.rotationDegrees] Optional Node3D rotation_degrees as [x,y,z] or { x, y, z }.
     * @param {*} [args.scale] Optional Node3D scale as [x,y,z] or { x, y, z }.
     * @param {*} [args.size] Box/Cube size as [x,y,z] or { x, y, z }; Plane/Quad size as [x,y] or { x, y }.
     * @param {number} [args.topRadius] Cylinder top_radius override.
     */
      "create_mesh_instance_3d": (args = {}) => call("create_mesh_instance_3d", args),
      /**
     * Create a RigidBody3D and optionally create a Shape3D resource plus CollisionShape3D child in one editor workflow.
     * @param {object} [args]
     * @param {number} [args.angularDamp] RigidBody3D angular_damp.
     * @param {boolean} [args.canSleep] Whether can_sleep is enabled.
     * @param {boolean} [args.collisionDisabled] Whether the CollisionShape3D child starts disabled.
     * @param {number} [args.collisionHeight] Capsule or cylinder collision height.
     * @param {number} [args.collisionMargin] Optional collision Shape3D margin.
     * @param {string} [args.collisionName] Optional CollisionShape3D child name.
     * @param {object} [args.collisionNodeProperties] Advanced Godot CollisionShape3D child properties merged after curated collision fields.
     * @param {*} [args.collisionPosition] Optional child CollisionShape3D position as [x,y,z] or { x, y, z }.
     * @param {number} [args.collisionRadius] Sphere, capsule, or cylinder collision radius.
     * @param {*} [args.collisionRotationDegrees] Optional child CollisionShape3D rotation_degrees as [x,y,z] or { x, y, z }.
     * @param {*} [args.collisionScale] Optional child CollisionShape3D scale as [x,y,z] or { x, y, z }.
     * @param {string} [args.collisionShapeKind] Optional child collision shape resource kind: box, sphere, capsule, or cylinder. Defaults to box whe
     * @param {string} [args.collisionShapePath] Optional Shape3D resource path to create and attach as a CollisionShape3D child.
     * @param {object} [args.collisionShapeProperties] Advanced Godot Shape3D properties merged after curated collision fields.
     * @param {*} [args.collisionSize] BoxShape3D child size as [x,y,z] or { x, y, z }.
     * @param {boolean} [args.contactMonitor] Whether contact_monitor is enabled.
     * @param {boolean} [args.continuousCd] Whether continuous_cd is enabled.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {boolean} [args.freeze] Whether the body starts frozen.
     * @param {number} [args.gravityScale] RigidBody3D gravity_scale.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.linearDamp] RigidBody3D linear_damp.
     * @param {boolean} [args.lockRotation] Whether lock_rotation is enabled.
     * @param {number} [args.mass] RigidBody3D mass.
     * @param {number} [args.maxContactsReported] RigidBody3D max_contacts_reported.
     * @param {string} [args.name] Optional RigidBody3D node name.
     * @param {boolean} [args.openCollisionShape] Open the collision shape resource in the visible editor after creation. Defaults to false.
     * @param {boolean} [args.overwriteCollisionShape] Overwrite an existing collision shape resource. Defaults to false.
     * @param {string} [args.parentPath] Path under the edited scene root. Empty string means the scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {*} [args.position] Optional Node3D position as [x,y,z] or { x, y, z }.
     * @param {object} [args.properties] Advanced Godot RigidBody3D node properties merged after curated fields.
     * @param {*} [args.rotationDegrees] Optional Node3D rotation_degrees as [x,y,z] or { x, y, z }.
     * @param {*} [args.scale] Optional Node3D scale as [x,y,z] or { x, y, z }.
     * @param {boolean} [args.sleeping] Whether the body starts sleeping.
     */
      "create_rigid_body_3d": (args = {}) => call("create_rigid_body_3d", args),
      /**
     * Create a StaticBody3D world collision body and optionally create a Shape3D resource plus CollisionShape3D child.
     * @param {object} [args]
     * @param {boolean} [args.collisionDisabled] Whether the CollisionShape3D child starts disabled.
     * @param {number} [args.collisionHeight] Capsule or cylinder collision height.
     * @param {number} [args.collisionLayer] CollisionObject3D collision_layer bitmask.
     * @param {number} [args.collisionMargin] Optional collision Shape3D margin.
     * @param {number} [args.collisionMask] CollisionObject3D collision_mask bitmask.
     * @param {string} [args.collisionName] Optional CollisionShape3D child name.
     * @param {object} [args.collisionNodeProperties] Advanced Godot CollisionShape3D child properties merged after curated collision fields.
     * @param {*} [args.collisionPosition] Optional child CollisionShape3D position as [x,y,z] or { x, y, z }.
     * @param {number} [args.collisionRadius] Sphere, capsule, or cylinder collision radius.
     * @param {*} [args.collisionRotationDegrees] Optional child CollisionShape3D rotation_degrees as [x,y,z] or { x, y, z }.
     * @param {*} [args.collisionScale] Optional child CollisionShape3D scale as [x,y,z] or { x, y, z }.
     * @param {string} [args.collisionShapeKind] Optional child collision shape resource kind: box, sphere, capsule, or cylinder. Defaults to box whe
     * @param {string} [args.collisionShapePath] Optional Shape3D resource path to create and attach as a CollisionShape3D child.
     * @param {object} [args.collisionShapeProperties] Advanced Godot Shape3D properties merged after curated collision fields.
     * @param {*} [args.collisionSize] BoxShape3D child size as [x,y,z] or { x, y, z }.
     * @param {*} [args.constantAngularVelocity] StaticBody3D constant_angular_velocity as [x,y,z] or { x, y, z }.
     * @param {*} [args.constantLinearVelocity] StaticBody3D constant_linear_velocity as [x,y,z] or { x, y, z }.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.name] Optional StaticBody3D node name.
     * @param {boolean} [args.openCollisionShape] Open the collision shape resource in the visible editor after creation. Defaults to false.
     * @param {boolean} [args.overwriteCollisionShape] Overwrite an existing collision shape resource. Defaults to false.
     * @param {string} [args.parentPath] Path under the edited scene root. Empty string means the scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {*} [args.position] Optional Node3D position as [x,y,z] or { x, y, z }.
     * @param {object} [args.properties] Advanced Godot StaticBody3D node properties merged after curated fields.
     * @param {*} [args.rotationDegrees] Optional Node3D rotation_degrees as [x,y,z] or { x, y, z }.
     * @param {*} [args.scale] Optional Node3D scale as [x,y,z] or { x, y, z }.
     */
      "create_static_body_3d": (args = {}) => call("create_static_body_3d", args),
    },
    "nodes-common": {
      /**
     * Create a node in the current edited Godot scene.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.name] Optional node name.
     * @param {string} [args.parentPath] Path under the edited scene root. Empty string means the scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {object} [args.properties] Optional initial node properties.
     * @param {string} args.type Godot class name to instantiate, for example Node3D, MeshInstance3D, or Camera3D.
     */
      "create_node": (args = {}) => call("create_node", args),
      /**
     * Create a Godot scene node and create or attach a GDScript in one editor operation.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.name] Optional node name.
     * @param {boolean} [args.overwriteScript] Overwrite an existing script before attaching it. Defaults to false.
     * @param {string} [args.parentPath] Path under the edited scene root. Empty string means the scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {object} [args.properties] Optional initial node properties.
     * @param {boolean} [args.saveScene] Save the current edited scene after attaching the script. Defaults to false.
     * @param {string} [args.scriptBaseType] Base type used for generated script content. Defaults to the created node type.
     * @param {string} [args.scriptClassName] Optional GDScript class_name used when generated script content is created.
     * @param {string} [args.scriptContent] Optional full GDScript content. When omitted and the script is created, the bridge writes the select
     * @param {string} args.scriptPath GDScript path under res:// to create or attach to the new node.
     * @param {string} [args.scriptTemplate] Generated template used when the script is created and scriptContent is omitted. Defaults to extends
     * @param {string} args.type Godot class name to instantiate, for example Node3D, MeshInstance3D, or Camera3D.
     */
      "create_node_with_script": (args = {}) => call("create_node_with_script", args),
      /**
     * Delete a non-root node from the current edited Godot scene.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.nodePath Path under the edited scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "delete_node": (args = {}) => call("delete_node", args),
      /**
     * Duplicate a node in the current edited Godot scene.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.newName] Optional name for the duplicate node.
     * @param {string} args.nodePath Path under the edited scene root.
     * @param {string} [args.parentPath] Optional destination parent. Defaults to the source node's parent.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "duplicate_node": (args = {}) => call("duplicate_node", args),
      /**
     * Rename a node in the current edited Godot scene.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.newName New node name.
     * @param {string} args.nodePath Path under the edited scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "rename_node": (args = {}) => call("rename_node", args),
      /**
     * Move a node to a target sibling index under its current parent in the current edited Godot scene.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} args.index Target sibling index under the node's current parent. Zero moves the node to the top of the Scene do
     * @param {string} args.nodePath Path under the edited scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "reorder_node": (args = {}) => call("reorder_node", args),
      /**
     * Move a node under a new parent in the current edited Godot scene.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {boolean} [args.keepGlobalTransform] Preserve Node2D/Node3D global transform where possible. Defaults to true.
     * @param {string} args.newParentPath Destination parent path under the edited scene root.
     * @param {string} args.nodePath Path under the edited scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "reparent_node": (args = {}) => call("reparent_node", args),
      /**
     * Search Godot ClassDB node types for Create Node dialog workflows, including instantiability, enabled-state, and inheritance metadata.
     * @param {object} [args]
     * @param {string} [args.baseType] Godot base class all returned classes must inherit from. Defaults to Node.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {boolean} [args.includeAbstract] Include abstract or uninstantiable classes. Defaults to false.
     * @param {boolean} [args.includeDisabled] Include ClassDB classes disabled in this Godot build. Defaults to false.
     * @param {number} [args.limit] Maximum number of matches to return. Clamped to 1..500. Defaults to 50.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} [args.query] Case-insensitive node class name search text.
     */
      "search_node_types": (args = {}) => call("search_node_types", args),
    },
    "particles": {
      /**
     * Create or update the ParticleProcessMaterial on a GPUParticles2D or GPUParticles3D node.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {object} args.material
     * @param {string} args.nodePath GPUParticles2D or GPUParticles3D node path under the edited scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.replace] Create a fresh ParticleProcessMaterial instead of reusing the current one.
     */
      "configure_particle_process_material": (args = {}) => call("configure_particle_process_material", args),
      /**
     * Create a GPUParticles2D emitter with a default texture and optional ParticleProcessMaterial settings.
     * @param {object} [args]
     * @param {number} [args.amount]
     * @param {boolean} [args.emitting]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.lifetime]
     * @param {object} [args.material]
     * @param {string} [args.name]
     * @param {boolean} [args.oneShot]
     * @param {string} [args.parentPath] Parent path under the edited scene root. Empty string means the scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {object} [args.position]
     * @param {number} [args.preprocess]
     * @param {object} [args.scale]
     * @param {number} [args.textureSize]
     */
      "create_gpu_particles_2d": (args = {}) => call("create_gpu_particles_2d", args),
      /**
     * Create a GPUParticles3D emitter with a draw-pass mesh and optional ParticleProcessMaterial settings.
     * @param {object} [args]
     * @param {number} [args.amount]
     * @param {boolean} [args.emitting]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.lifetime]
     * @param {object} [args.material]
     * @param {number} [args.meshRadius]
     * @param {object} [args.meshSize]
     * @param {string} [args.meshType] Draw-pass mesh type. Defaults to sphere.
     * @param {string} [args.name]
     * @param {boolean} [args.oneShot]
     * @param {string} [args.parentPath] Parent path under the edited scene root. Empty string means the scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {object} [args.position]
     * @param {number} [args.preprocess]
     * @param {object} [args.quadSize]
     * @param {object} [args.rotationDegrees]
     * @param {object} [args.scale]
     */
      "create_gpu_particles_3d": (args = {}) => call("create_gpu_particles_3d", args),
      /**
     * Set a GPUParticles2D or GPUParticles3D node's emitting and one-shot state, optionally restarting it.
     * @param {object} [args]
     * @param {boolean} [args.emitting]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.nodePath GPUParticles2D or GPUParticles3D node path under the edited scene root.
     * @param {boolean} [args.oneShot]
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.restart] Call restart() after changing state.
     */
      "set_particles_emitting": (args = {}) => call("set_particles_emitting", args),
    },
    "playable2d-workflow": {
      /**
     * Create default A/D/space input actions, write a curated CharacterBody2D movement script, and attach it to a player node.
     * @param {object} [args]
     * @param {object} [args.actionNames] Optional action name overrides used by both the Input Map and generated script.
     * @param {string} [args.className] Optional GDScript class_name. Defaults to <NodeName>Controller2D.
     * @param {boolean} [args.configureInputMap] Create or replace default A/D/space input actions before writing the script. Defaults to true.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {number} [args.gravity] Gravity applied while airborne. Defaults to 1400.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.jumpVelocity] Jump velocity. Defaults to -540.
     * @param {number} [args.moveSpeed] Horizontal movement speed in pixels per second. Defaults to 360.
     * @param {string} args.nodePath CharacterBody2D node path under the edited scene root.
     * @param {boolean} [args.overwriteScript] Overwrite the target script if it already exists. Defaults to false.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.saveScene] Save the current scene after attaching the script. Defaults to true.
     * @param {string} [args.scriptPath] GDScript path under res://. Defaults to res://scripts/<node>_controller_2d.gd.
     * @param {boolean} [args.validateAfterCreate] Validate the generated script before attaching it. Defaults to true.
     */
      "create_2d_character_controller": (args = {}) => call("create_2d_character_controller", args),
      /**
     * Compose a practical 2D playable scene blockout with root, ground collision, player body, visible sprites, camera, inputs, and controller script.
     * @param {object} [args]
     * @param {object} [args.actionNames] Optional action name overrides used by both the Input Map and generated script.
     * @param {string} [args.cameraName] Name for the Camera2D child under the player body. Defaults to PlayerCamera.
     * @param {*} [args.cameraPosition] Camera2D local position as [x,y] or { x, y }. Defaults to [0,-80].
     * @param {*} [args.cameraZoom] Camera2D zoom as [x,y] or { x, y }. Defaults to [1,1].
     * @param {string} [args.className] Optional GDScript class_name. Defaults to <NodeName>Controller2D.
     * @param {boolean} [args.configureInputMap] Create or replace default A/D/space input actions before writing the script. Defaults to true.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {number} [args.gravity] Gravity applied while airborne. Defaults to 1400.
     * @param {string} [args.groundBodyName] Name for the StaticBody2D ground body. Defaults to GroundBody.
     * @param {string} [args.groundCollisionName] Name for the ground CollisionShape2D child. Defaults to GroundCollision.
     * @param {*} [args.groundPosition] Ground body position as [x,y] or { x, y }. Defaults to [0,0].
     * @param {*} [args.groundSize] Ground rectangle size as [x,y] or { x, y }. Defaults to [640,48].
     * @param {string} [args.groundVisualName] Name for the visible ground Sprite2D child. Defaults to GroundVisual.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.jumpVelocity] Jump velocity. Defaults to -540.
     * @param {number} [args.moveSpeed] Horizontal movement speed in pixels per second. Defaults to 360.
     * @param {boolean} [args.overwriteResources] Overwrite generated shape and placeholder texture resources. Defaults to false.
     * @param {boolean} [args.overwriteScript] Overwrite the generated controller script. Defaults to overwriteResources.
     * @param {string} [args.parentPath] Path under the edited scene root where the blockout root should be created.
     * @param {string} [args.playerCollisionName] Name for the player CollisionShape2D child. Defaults to PlayerCollision.
     * @param {string} [args.playerName] Name for the CharacterBody2D player body. Defaults to PlayerBody.
     * @param {*} [args.playerPosition] Player body position as [x,y] or { x, y }. Defaults to [0,-80].
     * @param {*} [args.playerSize] Player capsule/sprite size as [x,y] or { x, y }. Defaults to [32,64].
     * @param {string} [args.playerVisualName] Name for the visible player Sprite2D child. Defaults to PlayerVisual.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} [args.resourceDirectory] res:// directory for generated 2D shape and placeholder texture resources.
     * @param {string} [args.rootName] Name for the generated Node2D blockout root. Defaults to PlayableBlockout2D.
     * @param {object} [args.rootProperties] Advanced Godot Node2D properties merged into the blockout root.
     * @param {boolean} [args.saveScene] Save the current scene after attaching the script. Defaults to true.
     * @param {string} [args.scriptPath] GDScript path under res://. Defaults to res://scripts/<node>_controller_2d.gd.
     * @param {boolean} [args.validateAfterCreate] Validate the generated script before attaching it. Defaults to true.
     */
      "create_2d_playable_blockout": (args = {}) => call("create_2d_playable_blockout", args),
      /**
     * Create an Area2D trigger zone with collision, an optional visible Sprite2D helper, and a generated runtime-probe script.
     * @param {object} [args]
     * @param {boolean} [args.attachScript] Write and attach the generated trigger script. Defaults to true.
     * @param {string} [args.className] Optional GDScript class_name. Defaults to <AreaName>Trigger2D.
     * @param {boolean} [args.collisionDisabled] Whether the CollisionShape2D child starts disabled.
     * @param {number} [args.collisionLayer] Area2D collision_layer integer.
     * @param {number} [args.collisionMask] Area2D collision_mask integer.
     * @param {string} [args.collisionName] Name for the CollisionShape2D child. Defaults to <name>Collision.
     * @param {object} [args.collisionNodeProperties] Advanced Godot CollisionShape2D properties merged after curated fields.
     * @param {*} [args.collisionPosition] Optional CollisionShape2D local position as [x,y] or { x, y }.
     * @param {number} [args.collisionRotationDegrees] Optional CollisionShape2D rotation_degrees value.
     * @param {*} [args.collisionScale] Optional CollisionShape2D local scale as [x,y] or { x, y }.
     * @param {string} [args.collisionShapePath] Optional Shape2D resource output path. Defaults under resourceDirectory.
     * @param {object} [args.collisionShapeProperties] Advanced Godot Shape2D properties merged after curated fields.
     * @param {boolean} [args.createVisual] Create a visible Sprite2D helper child. Defaults to false.
     * @param {string} [args.eventName] Event name emitted by the generated trigger script. Defaults to a slug of name.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {number} [args.height] CapsuleShape2D height.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {boolean} [args.monitorable] Whether other areas can monitor this Area2D. Defaults to true.
     * @param {boolean} [args.monitoring] Whether Area2D monitoring is enabled. Defaults to true.
     * @param {string} [args.name] Name for the generated Area2D trigger. Defaults to TriggerZone.
     * @param {boolean} [args.overwriteCollisionShape] Overwrite an existing Shape2D resource. Defaults to overwriteResources.
     * @param {boolean} [args.overwriteResources] Overwrite generated trigger resources. Defaults to false.
     * @param {boolean} [args.overwriteScript] Overwrite the generated trigger script. Defaults to false.
     * @param {boolean} [args.overwriteVisualTexture] Overwrite the optional visual placeholder texture resource. Defaults to overwriteResources.
     * @param {string} [args.parentPath] Path under the edited scene root where the Area2D should be created.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {*} [args.position] Area2D position as [x,y] or { x, y }.
     * @param {boolean} [args.printEvents] Print trigger events when the Niua runtime probe is absent. Defaults to true.
     * @param {number} [args.priority] Area2D priority.
     * @param {object} [args.properties] Advanced Godot Area2D properties merged after curated fields.
     * @param {number} [args.radius] CircleShape2D or CapsuleShape2D radius.
     * @param {string} [args.resourceDirectory] res:// directory for generated trigger Shape2D and optional placeholder texture resources.
     * @param {number} [args.rotationDegrees] Area2D rotation_degrees value.
     * @param {boolean} [args.saveScene] Save the current scene after attaching the script. Defaults to true.
     * @param {*} [args.scale] Area2D scale as [x,y] or { x, y }.
     * @param {string} [args.scriptPath] GDScript path under res://. Defaults to res://scripts/<area>_trigger_2d.gd.
     * @param {string} [args.shapeKind] Trigger shape kind: rectangle, circle, or capsule. Defaults to rectangle.
     * @param {*} [args.size] RectangleShape2D size as [x,y] or { x, y }. Defaults to [64,64].
     * @param {boolean} [args.validateAfterCreate] Validate the generated script before attaching it. Defaults to true.
     * @param {string} [args.visualName] Name for the optional visible Sprite2D child. Defaults to <name>Visual.
     * @param {string} [args.visualPlaceholderTexturePath] Optional generated PlaceholderTexture2D output path. Defaults under resourceDirectory.
     * @param {*} [args.visualPosition] Optional Sprite2D local position as [x,y] or { x, y }.
     * @param {object} [args.visualProperties] Advanced Godot Sprite2D properties merged after curated fields.
     * @param {number} [args.visualRotationDegrees] Optional Sprite2D local rotation_degrees value.
     * @param {*} [args.visualScale] Optional Sprite2D local scale as [x,y] or { x, y }.
     * @param {*} [args.visualSize] PlaceholderTexture2D size as [x,y] or { x, y }. Defaults to size.
     * @param {string} [args.visualTexturePath] Existing texture resource path assigned to the optional Sprite2D child.
     * @param {boolean} [args.watchAreas] Connect area_entered/area_exited in the generated script. Defaults to false.
     * @param {boolean} [args.watchBodies] Connect body_entered/body_exited in the generated script. Defaults to true.
     */
      "create_2d_trigger_zone": (args = {}) => call("create_2d_trigger_zone", args),
    },
    "playable3d-workflow": {
      /**
     * Create default WASD/space input actions, write a curated CharacterBody3D movement script, and attach it to a player node.
     * @param {object} [args]
     * @param {object} [args.actionNames] Optional action name overrides used by both the Input Map and generated script.
     * @param {string} [args.className] Optional GDScript class_name. Defaults to <NodeName>Controller3D.
     * @param {boolean} [args.configureInputMap] Create or replace default WASD/space input actions before writing the script. Defaults to true.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {number} [args.gravity] Gravity applied while airborne. Defaults to 9.8.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.jumpVelocity] Jump velocity. Defaults to 4.5.
     * @param {string} args.nodePath CharacterBody3D node path under the edited scene root.
     * @param {boolean} [args.overwriteScript] Overwrite the target script if it already exists. Defaults to false.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.saveScene] Save the current scene after attaching the script. Defaults to true.
     * @param {string} [args.scriptPath] GDScript path under res://. Defaults to res://scripts/<node>_controller_3d.gd.
     * @param {number} [args.speed] Movement speed in meters per second. Defaults to 7.
     * @param {boolean} [args.validateAfterCreate] Validate the generated script before attaching it. Defaults to true.
     */
      "create_3d_character_controller": (args = {}) => call("create_3d_character_controller", args),
      /**
     * Compose a practical 3D playable scene blockout with root, ground mesh/collision, player body, visual capsule, chase camera, and key light.
     * @param {object} [args]
     * @param {number} [args.cameraFov] Camera3D FOV. Defaults to 70.
     * @param {string} [args.cameraName] Name for the Camera3D child under the player body. Defaults to ChaseCamera.
     * @param {*} [args.cameraPosition] Camera3D local position as [x,y,z] or { x, y, z }. Defaults to [0, 2.4, 6].
     * @param {*} [args.cameraRotationDegrees] Camera3D local rotation_degrees as [x,y,z] or { x, y, z }. Defaults to [-18, 0, 0].
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.groundBodyName] Name for the StaticBody3D ground collision body. Defaults to GroundBody.
     * @param {string} [args.groundCollisionName] Name for the ground CollisionShape3D child. Defaults to GroundCollision.
     * @param {*} [args.groundPosition] Ground center position as [x,y,z] or { x, y, z }. Defaults to y = -groundSize.y / 2.
     * @param {*} [args.groundSize] Ground box size as [x,y,z] or { x, y, z }. Defaults to [24, 0.4, 24].
     * @param {string} [args.groundVisualName] Name for the visible ground MeshInstance3D. Defaults to GroundVisual.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.lightEnergy] DirectionalLight3D light_energy. Defaults to 1.6.
     * @param {string} [args.lightName] Name for the DirectionalLight3D. Defaults to KeyLight.
     * @param {*} [args.lightRotationDegrees] DirectionalLight3D rotation_degrees as [x,y,z] or { x, y, z }. Defaults to [-55, -35, 0].
     * @param {boolean} [args.overwriteResources] Overwrite generated mesh and shape resources. Defaults to false.
     * @param {string} [args.parentPath] Path under the edited scene root where the blockout root should be created.
     * @param {string} [args.playerCollisionName] Name for the player CollisionShape3D child. Defaults to PlayerCollision.
     * @param {number} [args.playerHeight] Player capsule height. Defaults to 1.8.
     * @param {string} [args.playerName] Name for the CharacterBody3D player body. Defaults to PlayerBody.
     * @param {*} [args.playerPosition] Player body position as [x,y,z] or { x, y, z }. Defaults to [0, playerHeight / 2, 0].
     * @param {number} [args.playerRadius] Player capsule radius. Defaults to 0.45.
     * @param {string} [args.playerVisualName] Name for the visible player MeshInstance3D. Defaults to PlayerVisual.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} [args.resourceDirectory] res:// directory for generated mesh and collision resources. Defaults to res://niua/generated/blocko
     * @param {string} [args.rootName] Name for the generated Node3D blockout root. Defaults to PlayableBlockout.
     * @param {object} [args.rootProperties] Advanced Godot Node3D properties merged into the blockout root.
     */
      "create_3d_playable_blockout": (args = {}) => call("create_3d_playable_blockout", args),
    },
    "playtest-workflow": {
      /**
     * One call playtest job: ensure main/custom scene, run, install runtime probe, read compact state/events, capture screenshot (or headless available:false), stop, return engine evidence pack. Prefer this over hand-rolling run/probe loops.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.maxDepth] Runtime tree maxDepth. Default 2.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.saveBeforeRun] Save edited scene before run. Default true.
     * @param {string} [args.savePath] Optional disk path for runtime screenshot PNG (keeps base64 out of context).
     * @param {string} [args.scenePath] Optional res:// scene to run (custom). If omitted, runs the project main scene.
     * @param {number} [args.settleMs] Wait after run before observing (ms). Default 250.
     * @param {boolean} [args.stopAfter] Stop the running scene after observe. Default true.
     */
      "run_playtest_evidence": (args = {}) => call("run_playtest_evidence", args),
    },
    "project-management": {
      /**
     * Terminate a Godot editor process launched by open_project.
     * @param {object} [args]
     * @param {string} [args.projectId] Tracked project process id returned by open_project.
     * @param {string} [args.projectRoot] Allowlisted project root to close when projectId is not provided.
     * @param {string} [args.signal] Process signal to send first. Defaults to SIGTERM.
     * @param {number} [args.timeoutMs] Milliseconds to wait before escalating to SIGKILL. Defaults to 3000.
     */
      "close_project": (args = {}) => call("close_project", args),
      /**
     * Create a local Godot project under an allowed root and optionally install the NIUA editor bridge addon.
     * @param {object} [args]
     * @param {boolean} [args.installAddon] Install and enable the local NIUA Godot MCP editor addon. Defaults to true.
     * @param {string} [args.name] Godot project display name. Defaults to the project directory name.
     * @param {boolean} [args.overwrite] Overwrite an existing project.godot file. Defaults to false.
     * @param {string} args.projectRoot Filesystem path for the Godot project. Must be inside GODOT_MCP_ALLOWED_PROJECT_ROOTS, or repo runs/
     */
      "create_project": (args = {}) => call("create_project", args),
      /**
     * Inspect whether an allowlisted Godot project has the NIUA MCP addon installed and enabled.
     * @param {object} [args]
     * @param {string} [args.bridgeHost] NIUA editor bridge host to probe. Defaults to GODOT_MCP_HOST or 127.0.0.1.
     * @param {number} [args.bridgePort] NIUA editor bridge port to probe. Defaults to GODOT_MCP_PORT or 9174.
     * @param {boolean} [args.checkBridge] Also probe the configured NIUA bridge health endpoint. Defaults to false.
     * @param {string} args.projectRoot Allowlisted Godot project root to inspect.
     * @param {number} [args.timeoutMs] Bridge health timeout in milliseconds. Defaults to 1000.
     */
      "diagnose_project_setup": (args = {}) => call("diagnose_project_setup", args),
      /**
     * Probe local NIUA Godot editor bridge ports and map active bridges to projects.
     * @param {object} [args]
     * @param {number} [args.endPort] Last bridge port to probe when ports is omitted. Defaults to 9194.
     * @param {string} [args.host] Local Godot bridge host to probe. Defaults to GODOT_MCP_HOST or 127.0.0.1.
     * @param {boolean} [args.includeUnavailable] Return failed probe diagnostics. Defaults to false.
     * @param {Array} [args.ports] Explicit bridge ports to probe. Defaults to GODOT_MCP_DISCOVERY_PORTS or 9174..9194.
     * @param {number} [args.startPort] First bridge port to probe when ports is omitted. Defaults to 9174.
     * @param {number} [args.timeoutMs] Per-request timeout in milliseconds. Defaults to 500.
     */
      "discover_editor_bridges": (args = {}) => call("discover_editor_bridges", args),
      /**
     * Scan allowlisted filesystem roots for Godot project.godot files and optionally remember them. Directories are walked in sorted name order, so results list deterministically.
     * @param {object} [args]
     * @param {number} [args.maxDepth] Maximum directory depth to scan under each root. Defaults to 4 and is clamped to 1..8.
     * @param {boolean} [args.remember] Persist discovered projects in the local NIUA project registry with source discovered. Defaults to f
     * @param {Array} [args.roots] Optional filesystem roots to scan. Every root must be inside GODOT_MCP_ALLOWED_PROJECT_ROOTS.
     */
      "discover_projects": (args = {}) => call("discover_projects", args),
      /**
     * Remove an allowlisted Godot project from the local NIUA project registry.
     * @param {object} [args]
     * @param {string} args.projectRoot Allowlisted project root to remove from the local NIUA project registry.
     */
      "forget_project": (args = {}) => call("forget_project", args),
      /**
     * List Godot editor processes launched by this MCP server.
     * @param {object} [args]
     * @param {boolean} [args.activeOnly] Only return projects whose editor process is still running. Defaults to false.
     */
      "get_open_projects": (args = {}) => call("get_open_projects", args),
      /**
     * Read recent Godot bridge logs, runtime log events, and stdout/stderr captured from local Godot editor processes launched by this MCP server. Pass clearAfterRead: true to empty the process buffers after reading, so the next call only shows fresh output.
     * @param {object} [args]
     * @param {boolean} [args.clearAfterRead] When true, empty the captured stdout/stderr buffers of the selected processes after this read, so th
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {boolean} [args.includeBridge] Read bridge-local logs from the Godot editor plugin. Defaults to true.
     * @param {boolean} [args.includeProcess] Include stdout/stderr captured from Godot editor processes launched by this MCP server. Defaults to 
     * @param {number} [args.maxLines] Maximum recent lines per process stream to return. Defaults to 100.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} [args.projectId] Optional project process id returned by open_project. When omitted, process logs from all open proje
     * @param {string} [args.projectRoot] Optional allowed Godot project root to filter launched editor process logs.
     */
      "get_output_logs": (args = {}) => call("get_output_logs", args),
      /**
     * Add an existing allowlisted Godot project to the local NIUA project registry without opening it.
     * @param {object} [args]
     * @param {boolean} [args.installAddon] Install and enable the local NIUA Godot MCP editor addon while importing. Defaults to false.
     * @param {string} args.projectRoot Filesystem path for an existing Godot project to add to the local NIUA project registry.
     */
      "import_project": (args = {}) => call("import_project", args),
      /**
     * Install or repair the local NIUA Godot editor bridge addon for an existing allowlisted project.
     * @param {object} [args]
     * @param {string} args.projectRoot Filesystem path for an existing Godot project whose NIUA editor bridge addon should be installed or 
     */
      "install_project_addon": (args = {}) => call("install_project_addon", args),
      /**
     * List Godot projects persisted in the local NIUA project registry.
     * @param {object} [args]
     */
      "list_known_projects": (args = {}) => call("list_known_projects", args),
      /**
     * List .tscn and .scn scene files under an allowlisted Godot project root.
     * @param {object} [args]
     * @param {number} [args.maxScenes] Maximum scene files to return. Defaults to 500.
     * @param {string} args.projectRoot Allowlisted Godot project root to scan for scene files.
     * @param {boolean} [args.recursive] Scan nested directories. Defaults to true.
     * @param {string} [args.rootPath] res:// directory to scan. Defaults to res://.
     */
      "list_scenes": (args = {}) => call("list_scenes", args),
      /**
     * Launch a local Godot editor process for an allowlisted project and track its lifecycle.
     * @param {object} [args]
     * @param {string} [args.bridgeHost] NIUA editor bridge host to poll. Defaults to GODOT_MCP_HOST or 127.0.0.1.
     * @param {number} [args.bridgePort] NIUA editor bridge port to poll. Defaults to GODOT_MCP_PORT or 9174.
     * @param {boolean} [args.headless] Launch Godot with --headless before opening the editor. Defaults to false.
     * @param {boolean} [args.installAddon] Install and enable the local NIUA Godot MCP editor addon before launch. Defaults to true.
     * @param {string} args.projectRoot Filesystem path for an existing Godot project. Must be inside GODOT_MCP_ALLOWED_PROJECT_ROOTS, or re
     * @param {boolean} [args.reuseExisting] Return an existing running project process for the same root instead of launching another. Defaults 
     * @param {number} [args.timeoutMs] Bridge wait and close timeout in milliseconds. Defaults to 10000 for open.
     * @param {boolean} [args.waitForBridge] Poll the NIUA editor bridge health endpoint after launch. Defaults to true.
     */
      "open_project": (args = {}) => call("open_project", args),
    },
    "project-settings": {
      /**
     * Read the project's input actions from ProjectSettings — the same source set_input_action writes and the running game loads. Actions list in sorted name order; built-in ui_* actions appear only if the project overrides them.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "get_input_map": (args = {}) => call("get_input_map", args),
      /**
     * Read Godot Project Settings values, category tree, usage flags, and editor metadata, optionally filtered by prefix.
     * @param {object} [args]
     * @param {boolean} [args.basic] When set, include only settings whose basic Project Settings visibility matches this value.
     * @param {boolean} [args.editorVisible] When set, include only settings whose editor-visible flag matches this value.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {boolean} [args.internal] When set, include only settings whose internal flag matches this value.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} [args.prefix] Optional ProjectSettings key prefix, for example application/.
     * @param {string} [args.query] Optional case-insensitive search text matched against setting names, category/section/leaf labels, t
     * @param {boolean} [args.restartIfChanged] When set, include only settings whose restart-required flag matches this value.
     */
      "get_project_settings": (args = {}) => call("get_project_settings", args),
      /**
     * Create or replace a Godot Input Map action with keyboard or joypad button events.
     * @param {object} [args]
     * @param {number} [args.deadzone] Input action deadzone. Defaults to 0.2.
     * @param {Array} [args.events] Input event specs. Supports Input Map assignable events: { type: 'key' }, { type: 'action' }, { type
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.name Input action name.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.replace] Clear existing events before adding supplied events. Defaults to true.
     * @param {boolean} [args.save] Save project.godot after updating the action. Defaults to true.
     */
      "set_input_action": (args = {}) => call("set_input_action", args),
      /**
     * Set a Godot Project Settings value and optionally save project.godot.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.name ProjectSettings key, for example application/config/name.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.save] Save project.godot after setting the value. Defaults to true.
     * @param {*} args.value JSON value to store in ProjectSettings.
     */
      "set_project_setting": (args = {}) => call("set_project_setting", args),
      /**
     * Update Godot Project Settings metadata such as order, initial value, basic/internal visibility, and restart-required flags.
     * @param {object} [args]
     * @param {boolean} [args.basic] Whether this setting appears in basic Project Settings views.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {*} [args.initialValue] Initial/default JSON value for this ProjectSettings key.
     * @param {boolean} [args.internal] Whether this setting is marked internal.
     * @param {string} args.name ProjectSettings key, for example application/config/name.
     * @param {number} [args.order] Project Settings display order for this key.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.restartIfChanged] Whether changing this setting requires an editor/project restart.
     * @param {boolean} [args.save] Save project.godot after setting metadata. Defaults to true.
     */
      "set_project_setting_metadata": (args = {}) => call("set_project_setting_metadata", args),
    },
    "recipe-workflow": {
      /**
     * Execute a recipe JSON file of tool steps in one call. Per-step successes stay out of the response; only a compact summary and any failures return.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.maxSteps] Safety cap on executed steps. Defaults to 500.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} args.recipePath Path to a recipe JSON file on disk: absolute, or res:// relative to expectedProjectRoot. Shape: { na
     * @param {boolean} [args.stopOnError] Stop at the first failing step. Defaults to true.
     */
      "apply_scene_recipe": (args = {}) => call("apply_scene_recipe", args),
      /**
     * Execute up to 50 inline tool steps in one call — the inline sibling of apply_scene_recipe for small batches with no recipe file. Same denylist and compact summary: per-step successes stay out of the response.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {Array} args.steps Tool steps to execute in order, max 50. Use apply_scene_recipe with a recipe file for bigger batches
     * @param {boolean} [args.stopOnError] Stop at the first failing step. Defaults to true.
     */
      "batch_scene_operations": (args = {}) => call("batch_scene_operations", args),
    },
    "resources": {
      /**
     * Assign a saved Godot Material resource to a scene node material override or mesh surface.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.materialPath Godot material resource path under res://.
     * @param {string} args.nodePath Scene-tree node path under the edited scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {number} [args.surfaceIndex] Optional mesh surface index. When omitted, assigns material_override.
     */
      "assign_material": (args = {}) => call("assign_material", args),
      /**
     * Create a StandardMaterial3D asset from practical material fields and optionally assign it to a scene node.
     * @param {object} [args]
     * @param {*} [args.albedoColor] Base color as #RRGGBB, #RRGGBBAA, or { r, g, b, a } values in 0..1.
     * @param {number} [args.alpha] Optional alpha override for the albedo color.
     * @param {object} [args.assignToNode] Optional scene node assignment after creating the material.
     * @param {*} [args.baseColor] Alias for albedoColor.
     * @param {string} [args.className] Godot material class to create. Defaults to StandardMaterial3D.
     * @param {*} [args.cullMode] Cull mode: back, front, disabled, or the Godot enum integer.
     * @param {*} [args.emissionColor] Emission color as #RRGGBB, #RRGGBBAA, or { r, g, b, a } values in 0..1.
     * @param {boolean} [args.emissionEnabled] Whether emission is enabled. Automatically true when emissionColor is provided.
     * @param {number} [args.emissionEnergyMultiplier] StandardMaterial3D emission_energy_multiplier value.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.metallic] StandardMaterial3D metallic value.
     * @param {string} [args.name] Optional Godot resource_name for the saved material.
     * @param {boolean} [args.open] Open the material in the visible editor after creation. Defaults to true.
     * @param {boolean} [args.overwrite] Overwrite an existing material file. Defaults to false.
     * @param {string} args.path Godot material resource output path under res://, usually ending in .tres.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {object} [args.properties] Advanced Godot material properties merged after curated fields.
     * @param {number} [args.roughness] StandardMaterial3D roughness value.
     * @param {*} [args.shadingMode] Shading mode: per_pixel, per_vertex, unshaded, or the Godot enum integer.
     * @param {*} [args.transparency] Transparency mode: disabled, alpha, alpha_scissor, alpha_hash, alpha_depth_pre_pass, or the Godot en
     */
      "create_material": (args = {}) => call("create_material", args),
      /**
     * Create and save a Godot Resource-derived asset under res://, such as a material .tres.
     * @param {object} [args]
     * @param {string} args.className Godot Resource-derived class to instantiate, such as StandardMaterial3D.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {boolean} [args.open] Open the resource in the visible editor after creation. Defaults to true.
     * @param {boolean} [args.overwrite] Overwrite an existing resource file. Defaults to false.
     * @param {string} args.path Godot resource output path under res://.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {object} [args.properties] Optional resource properties encoded with NIUA variant JSON.
     */
      "create_resource": (args = {}) => call("create_resource", args),
      /**
     * Create a Shader resource plus ShaderMaterial resource, set shader uniform values, and optionally assign it to a scene node.
     * @param {object} [args]
     * @param {object} [args.assignToNode] Optional scene node assignment after creating the ShaderMaterial.
     * @param {string} args.code Godot shading language source code for the saved Shader resource.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.name] Alias for resourceName.
     * @param {boolean} [args.open] Open the ShaderMaterial in the visible editor after creation. Defaults to true.
     * @param {boolean} [args.overwrite] Overwrite an existing ShaderMaterial file. Defaults to false.
     * @param {boolean} [args.overwriteShader] Overwrite an existing Shader file. Defaults to false.
     * @param {object} [args.parameters] Shader uniform values keyed by exact uniform name. Values may use NIUA typed variant JSON.
     * @param {string} args.path ShaderMaterial resource output path under res://, usually ending in .tres.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} [args.resourceName] Optional Godot resource_name for the saved ShaderMaterial.
     * @param {string} args.shaderPath Shader resource output path under res://, usually ending in .gdshader.
     */
      "create_shader_material": (args = {}) => call("create_shader_material", args),
      /**
     * Create a SpriteFrames resource from named animations and existing Texture2D frame resources.
     * @param {object} [args]
     * @param {Array} args.animations Named SpriteFrames animations with Texture2D frames or sprite-sheet grids.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {boolean} [args.open] Open the SpriteFrames resource in the visible editor after creation. Defaults to true.
     * @param {boolean} [args.overwrite] Overwrite an existing SpriteFrames resource. Defaults to false.
     * @param {string} args.path SpriteFrames resource output path under res://, usually ending in .tres.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} [args.resourceName] Optional Godot resource_name for the SpriteFrames asset.
     */
      "create_sprite_frames": (args = {}) => call("create_sprite_frames", args),
      /**
     * Create a TileSet resource from existing Texture2D atlases, explicit tile coordinates, and generated atlas grids.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {boolean} [args.open] Open the TileSet resource in the visible editor after creation. Defaults to true.
     * @param {boolean} [args.overwrite] Overwrite an existing TileSet resource. Defaults to false.
     * @param {string} args.path TileSet resource output path under res://, usually ending in .tres.
     * @param {Array} [args.physicsLayers] Optional TileSet physics layers. A default layer is created when collision polygons are provided wit
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} [args.resourceName] Optional Godot resource_name for the TileSet asset.
     * @param {Array} args.sources TileSetAtlasSource entries backed by existing Texture2D atlases.
     * @param {Array} [args.terrainSets] Optional TileSet terrain sets used by per-tile terrain metadata.
     * @param {*} [args.tileSize] TileSet tile size. Defaults to [16,16].
     */
      "create_tile_set": (args = {}) => call("create_tile_set", args),
      /**
     * Reveal and inspect a resource path in the visible Godot editor when supported by the local editor API.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.path Godot project path under res://.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "focus_resource": (args = {}) => call("focus_resource", args),
      /**
     * Open a scene/resource/script in the visible Godot editor.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.path Godot project path under res://.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "open_resource": (args = {}) => call("open_resource", args),
      /**
     * Load, update, and save an existing Godot Resource asset under res://.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {boolean} [args.open] Open the resource in the visible editor after saving. Defaults to false.
     * @param {string} args.path Existing Godot resource path under res://.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {object} [args.properties] Optional resource properties encoded with NIUA variant JSON.
     */
      "save_resource": (args = {}) => call("save_resource", args),
    },
    "run": {
      /**
     * Read Godot run settings such as the configured main scene.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "get_run_settings": (args = {}) => call("get_run_settings", args),
      /**
     * Read whether the Godot editor is currently playing a scene.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "get_run_status": (args = {}) => call("get_run_status", args),
      /**
     * Reload the currently running Godot scene by stopping playback and starting the same scene again.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.saveBeforeRun] Save the edited scene before play. Defaults to true (prevents Godot Save As modal traps).
     */
      "reload_running_scene": (args = {}) => call("reload_running_scene", args),
      /**
     * Run the currently edited scene from the visible Godot editor.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.saveBeforeRun] Save the edited scene before play. Defaults to true (prevents Godot Save As modal traps).
     */
      "run_current_scene": (args = {}) => call("run_current_scene", args),
      /**
     * Run a specific scene path from the visible Godot editor.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.path Godot scene path to run, for example res://scenes/smoke.tscn.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.saveBeforeRun] Save the edited scene before play. Defaults to true (prevents Godot Save As modal traps).
     */
      "run_custom_scene": (args = {}) => call("run_custom_scene", args),
      /**
     * Run the project's main scene from the visible Godot editor.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.saveBeforeRun] Save the edited scene before play. Defaults to true (prevents Godot Save As modal traps).
     */
      "run_main_scene": (args = {}) => call("run_main_scene", args),
      /**
     * Set application/run/main_scene to a scene path through the visible Godot editor bridge.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.path Godot scene path under res:// to store as application/run/main_scene.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.save] Save project.godot after updating the main scene. Defaults to true.
     */
      "set_main_scene": (args = {}) => call("set_main_scene", args),
      /**
     * Stop the scene currently playing from the Godot editor.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "stop_running_scene": (args = {}) => call("stop_running_scene", args),
    },
    "runtime": {
      /**
     * Return the installed Godot executable version used by the local MCP server.
     * @param {object} [args]
     */
      "get_godot_version": (args = {}) => call("get_godot_version", args),
    },
    "scene": {
      /**
     * Close the current Godot scene tab, optionally switching to and saving a target scene first.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} [args.path] Optional scene path to close. When provided, the editor switches to it before closing.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.saveBeforeClose] Save the target scene before closing it. Defaults to false.
     */
      "close_scene": (args = {}) => call("close_scene", args),
      /**
     * Create a new scene file with a configurable root node and optionally open it in the visible Godot editor.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {boolean} [args.open] Open the newly created scene in the visible editor. Defaults to true.
     * @param {boolean} [args.overwrite] Overwrite an existing scene file. Defaults to false.
     * @param {string} args.path Scene path to create under res://, for example res://scenes/main.tscn.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} [args.rootName] Optional root node name. Defaults to the root type.
     * @param {string} [args.rootType] Godot node class to use as the scene root. Defaults to Node3D.
     */
      "create_scene": (args = {}) => call("create_scene", args),
      /**
     * Select and focus a scene-tree node in the visible Godot editor.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.nodePath Scene-tree node path under the edited scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "focus_node": (args = {}) => call("focus_node", args),
      /**
     * Read current visible Godot editor state from the NIUA editor plugin bridge.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "get_editor_state": (args = {}) => call("get_editor_state", args),
      /**
     * Read ordered open scene tabs and the current visible Godot scene tab.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "get_open_scene_tabs": (args = {}) => call("get_open_scene_tabs", args),
      /**
     * Read the active Godot project root and project metadata from the editor bridge.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "get_project_info": (args = {}) => call("get_project_info", args),
      /**
     * Read the current scene tree from the visible Godot editor.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.maxDepth] Maximum tree depth to return. 0 or omitted means the full tree; truncated nodes report childrenTrunc
     * @param {string} [args.pathFilter] Only return the subtree rooted at this node path under the scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "get_scene_tree": (args = {}) => call("get_scene_tree", args),
      /**
     * Read the current editor selection from the visible Godot editor, including per-node parent, owner, sibling, group, and metadata-key context.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "get_selection": (args = {}) => call("get_selection", args),
      /**
     * Mark the current or target Godot scene tab as unsaved for dirty-state workflows.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.path Scene path to activate, for example res://scenes/main.tscn.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "mark_scene_unsaved": (args = {}) => call("mark_scene_unsaved", args),
      /**
     * Open an existing scene in the visible Godot editor.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.path Godot resource path, for example res://scenes/main.tscn.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "open_scene": (args = {}) => call("open_scene", args),
      /**
     * Redo the current Godot editor scene/history action, optionally targeting a historyId from get_open_scene_tabs.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {number} [args.historyId] Optional editor undo history id from get_open_scene_tabs. Defaults to the current edited scene root 
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "redo_editor_action": (args = {}) => call("redo_editor_action", args),
      /**
     * Save the current edited Godot scene.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "save_current_scene": (args = {}) => call("save_current_scene", args),
      /**
     * Save the current edited scene to a new scene path under res://.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.path Godot resource path, for example res://scenes/main.tscn.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "save_scene_as": (args = {}) => call("save_scene_as", args),
      /**
     * Replace the visible Godot editor node selection with one or more scene-tree nodes.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {Array} args.nodePaths Scene-tree node paths under the edited scene root. Empty array clears selection.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "set_selection": (args = {}) => call("set_selection", args),
      /**
     * Switch the visible Godot editor to an open scene tab, opening it if needed.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.path Scene path to activate, for example res://scenes/main.tscn.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "switch_scene_tab": (args = {}) => call("switch_scene_tab", args),
      /**
     * Undo the current Godot editor scene/history action, optionally targeting a historyId from get_open_scene_tabs.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {number} [args.historyId] Optional editor undo history id from get_open_scene_tabs. Defaults to the current edited scene root 
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "undo_editor_action": (args = {}) => call("undo_editor_action", args),
    },
    "scripts": {
      /**
     * Attach a GDScript file to a node in the current edited Godot scene.
     * @param {object} [args]
     * @param {string} [args.baseType] Base type for createIfMissing script templates. Defaults to the node class.
     * @param {string} [args.className] Optional GDScript class_name used when createIfMissing generates script content.
     * @param {string} [args.content] Optional script content used when createIfMissing creates the script.
     * @param {boolean} [args.createIfMissing] Create the script before attaching it when the file does not exist. Defaults to false.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.nodePath Scene-tree node path under the edited scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.saveScene] Save the current edited scene after attaching the script. Defaults to false.
     * @param {string} args.scriptPath GDScript path under res:// to attach to the node.
     * @param {string} [args.template] Generated template used when createIfMissing creates the script and content is omitted. Defaults to 
     */
      "attach_script": (args = {}) => call("attach_script", args),
      /**
     * Create a GDScript file using supplied content or a generated template with optional class_name.
     * @param {object} [args]
     * @param {string} [args.baseType] Godot class extended by the generated script when content is omitted. Defaults to Node.
     * @param {string} [args.className] Optional GDScript class_name to include when generated content is used.
     * @param {string} [args.content] Optional full GDScript content. When omitted, the bridge writes the selected generated template.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {boolean} [args.overwrite] Overwrite an existing script file. Defaults to false.
     * @param {string} args.path GDScript path under res://, for example res://scripts/player.gd.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} [args.template] Generated script template to use when content is omitted. Defaults to extends_only.
     */
      "create_script": (args = {}) => call("create_script", args),
      /**
     * Run Godot's GDScript parser across explicit or discovered project scripts and return aggregate diagnostics.
     * @param {object} [args]
     * @param {number} [args.maxScripts] Maximum number of scripts to diagnose. Defaults to 100 and is capped at 500.
     * @param {Array} [args.paths] Optional explicit GDScript paths under res:// to diagnose.
     * @param {string} args.projectRoot Filesystem path for the Godot project. Must be inside GODOT_MCP_ALLOWED_PROJECT_ROOTS, or repo runs/
     * @param {string} [args.rootPath] Directory under res:// to scan recursively when paths is omitted. Defaults to res://.
     * @param {number} [args.timeoutMs] Godot parser timeout in milliseconds per script. Defaults to 10000.
     */
      "diagnose_project_scripts": (args = {}) => call("diagnose_project_scripts", args),
      /**
     * Run Godot's GDScript parser for a res:// script and return structured diagnostics.
     * @param {object} [args]
     * @param {string} args.path GDScript path under res:// to parse, for example res://scripts/player.gd.
     * @param {string} args.projectRoot Filesystem path for the Godot project. Must be inside GODOT_MCP_ALLOWED_PROJECT_ROOTS, or repo runs/
     * @param {number} [args.timeoutMs] Godot parser timeout in milliseconds. Defaults to 10000.
     */
      "diagnose_script": (args = {}) => call("diagnose_script", args),
      /**
     * Replace exact oldText with newText in a res:// GDScript file. oldText must be unique unless replaceAll:true; the edited file is parse-checked by default and valid:false means the edit WAS applied but the script no longer parses — fix or revert. replacements and totalLines are read back from disk.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.newText Replacement text. Must differ from oldText.
     * @param {string} args.oldText Exact text to replace, including whitespace.
     * @param {string} args.path GDScript path under res:// to edit.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.replaceAll] Replace every occurrence. Defaults to false; oldText must then be unique.
     * @param {boolean} [args.validate] Parse-check the edited script after writing. Defaults to true.
     */
      "edit_script": (args = {}) => call("edit_script", args),
      /**
     * Read active Godot Script Editor caret, selection, and visible-line metadata.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "get_script_cursor_state": (args = {}) => call("get_script_cursor_state", args),
      /**
     * Read visible Godot Script Editor state including current script, open scripts, and breakpoints.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "get_script_editor_state": (args = {}) => call("get_script_editor_state", args),
      /**
     * Read Godot script symbol metadata including methods, properties, signals, and constants.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.path Godot project path under res://.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "get_script_symbols": (args = {}) => call("get_script_symbols", args),
      /**
     * Open a script in the visible Godot Script Editor and focus a 1-based line number.
     * @param {object} [args]
     * @param {number} [args.column] 0-based column to focus. Defaults to 0.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {boolean} [args.grabFocus] Whether the script editor should grab focus. Defaults to true.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} args.line 1-based script line number to focus.
     * @param {string} args.path GDScript path under res://.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "goto_script_line": (args = {}) => call("goto_script_line", args),
      /**
     * Open a GDScript file in the visible Godot editor.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.path Godot project path under res://.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "open_script": (args = {}) => call("open_script", args),
      /**
     * Read a GDScript file from the Godot project. Optional lineStart (1-based) + lineCount return only that line range; totalLines is always reported.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.lineCount] Number of lines to return from lineStart. Omitted = through end of file.
     * @param {number} [args.lineStart] 1-based first line to return. Out-of-range fails naming totalLines.
     * @param {string} args.path Godot project path under res://.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "read_script": (args = {}) => call("read_script", args),
      /**
     * Preview or apply a capped literal replacement across GDScript files in the Godot project. When scanning by rootPath, files are visited in sorted name order per directory, so the maxFiles cap is deterministic.
     * @param {object} [args]
     * @param {boolean} [args.caseSensitive] Use case-sensitive literal matching. Defaults to true.
     * @param {boolean} [args.dryRun] Preview changes without writing files. Defaults to true.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.maxFiles] Maximum scripts to scan. Clamped by the bridge. Defaults to 200.
     * @param {number} [args.maxReplacements] Maximum replacements allowed before the operation aborts. Defaults to 1000.
     * @param {Array} [args.paths] Optional explicit GDScript paths under res://. When omitted, the bridge scans rootPath recursively.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} args.replacement Literal replacement text.
     * @param {string} [args.rootPath] Directory under res:// to scan when paths is omitted. Defaults to res://.
     * @param {string} args.search Literal text to search for in GDScript files. Regex is not supported.
     */
      "replace_in_scripts": (args = {}) => call("replace_in_scripts", args),
      /**
     * Search .gd files under a res:// prefix for plain-text (or Godot RegEx) matches. Returns matched lines only (trimmed to 160 chars) in sorted file order then line order; maxResults defaults to 50 (cap 200) with truncated:true when hit. Zero matches is a success.
     * @param {object} [args]
     * @param {boolean} [args.caseSensitive] Case-sensitive matching. Defaults to false.
     * @param {Array} [args.exclude] Path substrings to skip, such as ["addons", ".godot"].
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.maxResults] Maximum matches returned. Defaults to 50, capped at 200; truncated:true when hit.
     * @param {string} [args.pathPrefix] res:// directory to search. Defaults to res://.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} args.query Text to find in .gd files. Plain text unless regex is true.
     * @param {boolean} [args.regex] Treat query as a Godot RegEx pattern. Invalid patterns fail naming the pattern. Defaults to false.
     */
      "search_in_scripts": (args = {}) => call("search_in_scripts", args),
      /**
     * Validate that a GDScript file can be loaded by Godot.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.path Godot project path under res://.
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "validate_script": (args = {}) => call("validate_script", args),
      /**
     * Write a GDScript file under res:// and refresh the Godot editor filesystem.
     * @param {object} [args]
     * @param {string} args.content UTF-8 text content to write.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.path Godot project path under res://.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.refreshAfterWrite] Ask the Godot editor filesystem to refresh/import this path after writing. Defaults to true.
     */
      "write_script": (args = {}) => call("write_script", args),
    },
    "ui": {
      /**
     * Assign a Theme resource or per-node Control theme overrides such as font_size and font_color.
     * @param {object} [args]
     * @param {object} [args.colors] Theme colors keyed by item name. Values use JSON-encoded Color objects.
     * @param {object} [args.constants] Theme constants keyed by item name.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {object} [args.fontSizes] Theme font-size items keyed by theme item name, such as { font_size: 48 }.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.nodePath Control node path under the edited scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {object} [args.styleboxes] Theme StyleBoxFlat entries keyed by item name.
     * @param {string} [args.themePath] Optional Theme resource path to assign to the node.
     * @param {string} [args.themeTypeVariation] Optional Control theme_type_variation value.
     */
      "apply_ui_theme_override": (args = {}) => call("apply_ui_theme_override", args),
      /**
     * Connect a UI Control signal, for example Button.pressed, to a target script method.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {string} args.methodName Target script method name.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} args.signalName Signal to connect, such as pressed.
     * @param {string} args.sourcePath Source Control node path under the edited scene root.
     * @param {string} args.targetPath Target node path under the edited scene root.
     */
      "connect_ui_signal": (args = {}) => call("connect_ui_signal", args),
      /**
     * Create a supported Godot Control node such as Label, Button, Panel, TextureRect, or common containers.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {object} [args.layout]
     * @param {string} [args.name]
     * @param {string} [args.parentPath] Parent path under the edited scene root. Empty string means the scene root.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} [args.text] Text for Label or Button nodes.
     * @param {string} [args.texturePath] Texture resource path for TextureRect nodes.
     * @param {string} [args.tooltip] Control tooltip text.
     * @param {string} args.type Supported UI/Control node type to create.
     */
      "create_ui_control": (args = {}) => call("create_ui_control", args),
      /**
     * Create a Godot Theme resource with curated font-size, color, constant, and StyleBoxFlat entries.
     * @param {object} [args]
     * @param {string} [args.applyToNodePath] Optional Control node path to receive the created Theme.
     * @param {number} [args.defaultFontSize] Theme default font size.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {boolean} [args.overwrite]
     * @param {string} args.path Theme resource path under res://.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {Array} [args.typeStyles]
     */
      "create_ui_theme": (args = {}) => call("create_ui_theme", args),
      /**
     * Set anchors, offsets, minimum size, and size flags on a Godot Control node.
     * @param {object} [args]
     * @param {object} [args.anchors]
     * @param {object} [args.customMinimumSize]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.horizontalSizeFlags]
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {boolean} [args.keepOffsets] When using preset, preserve offsets. Defaults to false.
     * @param {string} args.nodePath Control node path under the edited scene root.
     * @param {object} [args.offsets]
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} [args.preset] Optional Godot Control layout preset.
     * @param {string} [args.verticalSizeFlags]
     */
      "set_control_layout": (args = {}) => call("set_control_layout", args),
    },
    "viewport": {
      /**
     * Capture a PNG screenshot of the visible Godot editor. Pass savePath to write the PNG to disk and keep large base64 payloads out of the tool result. Returns available=false when the active renderer cannot expose editor pixels, such as headless mode.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} [args.savePath] When set, decode the PNG payload and write it to this filesystem path instead of returning inline ba
     */
      "capture_editor_screenshot": (args = {}) => call("capture_editor_screenshot", args),
      /**
     * Capture a PNG screenshot from the Godot editor 2D or 3D viewport. Pass savePath to write the PNG to disk and keep large base64 payloads out of the tool result. Returns available=false when the active renderer cannot expose viewport pixels, such as headless mode.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.index] 3D viewport index. Ignored for 2D. Defaults to 0.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} [args.savePath] When set, decode the PNG payload and write it to this filesystem path instead of returning inline ba
     * @param {string} [args.viewport] Editor viewport to capture: 2d or 3d. Defaults to 3d.
     */
      "capture_viewport_screenshot": (args = {}) => call("capture_viewport_screenshot", args),
      /**
     * Read Godot editor 2D or 3D viewport size and active camera metadata when exposed by the editor.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.index] 3D viewport index. Ignored for 2D. Defaults to 0.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} [args.savePath] When set, decode the PNG payload and write it to this filesystem path instead of returning inline ba
     * @param {string} [args.viewport] Editor viewport to capture: 2d or 3d. Defaults to 3d.
     */
      "get_viewport_state": (args = {}) => call("get_viewport_state", args),
      /**
     * Invoke a conservative allowlisted Godot editor action such as select_file, filesystem_scan, reload_scene_from_path, save_scene, or set_distraction_free_mode.
     * @param {object} [args]
     * @param {string} args.action Allowlisted Godot editor action to invoke.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {object} [args.params] Action-specific parameters. Path parameters must be res:// paths and are validated by the Godot brid
     * @param {number} [args.port] Bridge port (default 9174).
     */
      "invoke_editor_action": (args = {}) => call("invoke_editor_action", args),
      /**
     * Send mouse pointer events into a Godot editor 2D or 3D viewport using viewport-local coordinates.
     * @param {object} [args]
     * @param {Array} args.events Pointer event specs. Supports mouse_motion, mouse_button, mouse_click, and mouse_drag.
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.index] 3D viewport index. Ignored for 2D. Defaults to 0.
     * @param {boolean} [args.local] When true, pointer positions are already in viewport coordinates. Defaults to true.
     * @param {boolean} [args.notifyMouseEntered] Call Viewport.notify_mouse_entered before sending events. Defaults to true.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {boolean} [args.updateMouseCursorState] Call Viewport.update_mouse_cursor_state after sending events. Defaults to true.
     * @param {string} [args.viewport] Editor viewport to receive input: 2d or 3d. Defaults to 3d.
     */
      "send_viewport_input": (args = {}) => call("send_viewport_input", args),
      /**
     * Switch the visible Godot editor main screen, such as 2D, 3D, Script, Game, or AssetLib.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {string} args.screen Godot editor main screen to activate. Common values are 2D, 3D, Script, Game, and AssetLib.
     */
      "set_editor_main_screen": (args = {}) => call("set_editor_main_screen", args),
      /**
     * Move the active Godot editor 2D or 3D viewport camera, then return updated camera metadata.
     * @param {object} [args]
     * @param {string} [args.expectedProjectRoot] Absolute project root the bridge must match; mismatch fails mutating/run tools.
     * @param {number} [args.far] 3D Camera3D far clipping plane.
     * @param {number} [args.fov] 3D Camera3D vertical field of view in degrees.
     * @param {string} [args.host] Bridge host (default 127.0.0.1).
     * @param {number} [args.index] 3D viewport index. Ignored for 2D. Defaults to 0.
     * @param {number} [args.near] 3D Camera3D near clipping plane.
     * @param {number} [args.port] Bridge port (default 9174).
     * @param {object} [args.position] Typed Vector2 for 2D or Vector3 for 3D, for example { type: 'Vector3', x: 1, y: 2, z: 3 }.
     * @param {*} [args.rotation] 2D rotation in radians, or 3D typed Vector3 Euler rotation in radians.
     * @param {*} [args.rotationDegrees] 2D rotation in degrees, or 3D typed Vector3 Euler rotation in degrees.
     * @param {string} [args.viewport] Editor viewport to mutate: 2d or 3d. Defaults to 3d.
     * @param {object} [args.zoom] 2D Camera2D zoom as typed Vector2, for example { type: 'Vector2', x: 1.5, y: 1.5 }.
     */
      "set_viewport_camera": (args = {}) => call("set_viewport_camera", args),
    },
  };
}
